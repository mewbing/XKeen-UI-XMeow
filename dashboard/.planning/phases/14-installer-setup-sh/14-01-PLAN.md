---
phase: 14-installer-setup-sh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - cmd/xmeow-server/main.go
autonomous: true
requirements: [INST-01, INST-02]
must_haves:
  truths:
    - "CI builds binary named xmeow-server (not xmeow-ui)"
    - "Release artifacts named xmeow-server_VERSION_ARCH.tar.gz"
    - "Binary supports --version flag printing version to stdout"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Release workflow with xmeow-server binary name"
      contains: "xmeow-server"
    - path: "cmd/xmeow-server/main.go"
      provides: "Go entrypoint with --version flag"
      contains: "--version"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "cmd/xmeow-server/"
      via: "go build path"
      pattern: "./cmd/xmeow-server/"
---

<objective>
Rename the CI binary from `xmeow-ui` to `xmeow-server` and add a `--version` flag to the Go entrypoint.

Purpose: The installer (setup.sh) needs the binary named `xmeow-server` (per user decision) and needs `xmeow-server --version` to check the currently installed version for update detection. These are prerequisites for the installer script.

Output: Updated release.yml + Go entrypoint with --version flag.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-installer-setup-sh/14-CONTEXT.md

<interfaces>
From cmd/xmeow-ui/main.go:
```go
var Version = "dev"

func main() {
    cfg := config.LoadConfig()
    cfg.Version = Version
    // ... server startup
}
```

From .github/workflows/release.yml:
- Binary built as: `go build -ldflags "..." -o xmeow-ui ./cmd/xmeow-ui/`
- Packaged as: `xmeow-ui_VERSION_SUFFIX.tar.gz`
- UPX compresses: `xmeow-ui`
- 5 matrix targets: arm64, mipsle, mips, amd64, armv7
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename cmd/xmeow-ui to cmd/xmeow-server and add --version flag</name>
  <files>cmd/xmeow-server/main.go</files>
  <action>
1. Rename directory `cmd/xmeow-ui/` to `cmd/xmeow-server/` (git mv).

2. Modify `cmd/xmeow-server/main.go` to add `--version` flag support:
   - At the top of `main()`, before any other logic, check `os.Args` for `--version` or `-v`:
     ```go
     if len(os.Args) > 1 && (os.Args[1] == "--version" || os.Args[1] == "-v") {
         fmt.Println(Version)
         os.Exit(0)
     }
     ```
   - Add `"fmt"` and `"os"` imports (os already imported via signal, fmt is new).
   - The output must be JUST the version string (e.g., "1.2.3" or "dev") with no prefix — the installer parses this output directly.

3. Verify the renamed package builds:
   ```
   go build ./cmd/xmeow-server/
   ```
  </action>
  <verify>
    <automated>cd /d/AI/antigravity/claude/config/mihomo/dashboard && go build -o /dev/null ./cmd/xmeow-server/ 2>&1 || echo "BUILD FAILED"</automated>
  </verify>
  <done>cmd/xmeow-server/main.go exists with --version flag, builds without errors, old cmd/xmeow-ui/ removed</done>
</task>

<task type="auto">
  <name>Task 2: Update release.yml to use xmeow-server binary name</name>
  <files>.github/workflows/release.yml</files>
  <action>
Replace ALL occurrences of `xmeow-ui` with `xmeow-server` in `.github/workflows/release.yml`:

1. **Build binary step:** `go build ... -o xmeow-server ./cmd/xmeow-server/`
2. **UPX step:** `files: xmeow-server`
3. **Package step:** `tar -czf xmeow-server_VERSION_SUFFIX.tar.gz xmeow-server README.md`
4. **Release name** remains "XMeow UI ${{ github.ref_name }}" (unchanged — this is the product name, not binary name)

Verify all 4 occurrences are renamed. The artifact pattern `binary-*` and `dist` names remain unchanged (they refer to GitHub Actions artifacts, not the binary name).

Do NOT change:
- The workflow `name: Release`
- The release title "XMeow UI ..."
- Artifact names (binary-*, dist)
- The dist.tar.gz packaging (it's for SPA files, not the binary)
  </action>
  <verify>
    <automated>cd /d/AI/antigravity/claude/config/mihomo/dashboard && grep -c "xmeow-ui" .github/workflows/release.yml | grep "^0$" && grep -c "xmeow-server" .github/workflows/release.yml | grep -q "[3-9]" && echo "OK" || echo "FAILED: xmeow-ui still present or xmeow-server missing"</automated>
  </verify>
  <done>release.yml has zero occurrences of "xmeow-ui", binary built as xmeow-server, packaged as xmeow-server_VER_ARCH.tar.gz</done>
</task>

</tasks>

<verification>
1. `grep -r "xmeow-ui" .github/ cmd/` returns no matches (full rename complete)
2. `go build ./cmd/xmeow-server/` succeeds
3. `ls cmd/xmeow-server/main.go` confirms new directory exists
4. `grep "version" cmd/xmeow-server/main.go` shows --version flag handling
</verification>

<success_criteria>
- Binary name is `xmeow-server` everywhere in CI and source
- Go binary supports `--version` flag printing bare version string
- No references to old `xmeow-ui` binary name remain in CI or cmd/
</success_criteria>

<output>
After completion, create `.planning/phases/14-installer-setup-sh/14-01-SUMMARY.md`
</output>
