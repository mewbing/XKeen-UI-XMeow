---
phase: 03-proxies-page
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/proxies/ProxyLatencyBadge.tsx
  - src/components/proxies/ProxyNodeItem.tsx
  - src/components/proxies/ProxyGroupCard.tsx
  - src/components/proxies/ProxiesToolbar.tsx
  - src/components/proxies/ProxiesSettingsPopover.tsx
  - src/pages/ProxiesPage.tsx
autonomous: true
requirements:
  - PROX-01
  - PROX-02
  - PROX-03

must_haves:
  truths:
    - "Все proxy-groups отображаются как карточки с текущим выбором, типом группы и настраиваемой плотностью информации"
    - "Настраиваемая сетка (1, 2 или 3 карточки в ряд) через поповер настроек"
    - "Раскрытие карточки на месте показывает полный список прокси группы с возможностью сортировки"
    - "Клик по прокси в раскрытой карточке Selector группы мгновенно переключает активный прокси с тостом"
    - "Автотест задержки при раскрытии карточки с кешированием 15 сек, цветовая маркировка (<100 зелёный, <300 жёлтый, остальное красный)"
    - "Массовый тест: кнопка «Тестировать все» и кнопка тест на каждой группе"
    - "Поиск по имени группы/прокси и фильтрация по типу группы"
    - "Для не-Selector групп (url-test, fallback) показан текущий автоматический выбор как read-only"
  artifacts:
    - path: "src/components/proxies/ProxyGroupCard.tsx"
      provides: "Карточка группы со свёрнутым и развёрнутым состоянием"
      min_lines: 80
    - path: "src/components/proxies/ProxyNodeItem.tsx"
      provides: "Элемент прокси в развёрнутом списке"
      min_lines: 20
    - path: "src/components/proxies/ProxyLatencyBadge.tsx"
      provides: "Бейдж задержки с цветовой маркировкой"
      min_lines: 15
    - path: "src/components/proxies/ProxiesToolbar.tsx"
      provides: "Тулбар: поиск, фильтр по типу, настройки, тест все"
      min_lines: 30
    - path: "src/components/proxies/ProxiesSettingsPopover.tsx"
      provides: "Поповер настроек (сетка, плотность, сортировка, стили типов)"
      min_lines: 40
    - path: "src/pages/ProxiesPage.tsx"
      provides: "Страница Proxies заменяет placeholder"
      min_lines: 40
  key_links:
    - from: "src/pages/ProxiesPage.tsx"
      to: "src/stores/proxies.ts"
      via: "useProxiesStore для данных и actions"
      pattern: "useProxiesStore"
    - from: "src/components/proxies/ProxyGroupCard.tsx"
      to: "src/stores/proxies.ts"
      via: "toggleExpand, selectProxyInGroup, testGroupDelay"
      pattern: "useProxiesStore"
    - from: "src/components/proxies/ProxiesSettingsPopover.tsx"
      to: "src/stores/settings.ts"
      via: "proxiesGridColumns, proxiesDensity, proxiesSort, proxiesTypeStyle"
      pattern: "useSettingsStore"
    - from: "src/components/proxies/ProxyLatencyBadge.tsx"
      to: "src/lib/format.ts"
      via: "formatDelay для отображения"
      pattern: "formatDelay"
---

<objective>
Создать все UI-компоненты страницы Proxies и заменить placeholder-страницу полноценной реализацией.

Purpose: Пользователь видит все proxy-groups как карточки, может переключать прокси, тестировать задержку, настраивать отображение.
Output: 5 новых компонентов в src/components/proxies/ и обновлённая ProxiesPage.tsx.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-proxies-page/03-CONTEXT.md
@.planning/phases/03-proxies-page/03-RESEARCH.md
@.planning/phases/03-proxies-page/03-01-SUMMARY.md
@src/stores/proxies.ts
@src/stores/settings.ts
@src/lib/mihomo-api.ts
@src/lib/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProxyLatencyBadge + ProxyNodeItem + ProxyGroupCard</name>
  <files>
    src/components/proxies/ProxyLatencyBadge.tsx
    src/components/proxies/ProxyNodeItem.tsx
    src/components/proxies/ProxyGroupCard.tsx
  </files>
  <action>
    **ProxyLatencyBadge.tsx** — Бейдж задержки с цветовой маркировкой:
    - Props: `delay: number | undefined`, `testing?: boolean`
    - Если `testing` — показать Skeleton (shadcn/ui) шириной ~40px
    - Цвета: delay === undefined → серый текст '--', delay === 0 → `text-destructive` 'timeout', delay < 100 → `text-green-500`, delay < 300 → `text-yellow-500`, иначе → `text-red-500`
    - Использовать `formatDelay` из `@/lib/format`
    - Обёртка: `<span className={cn("text-xs font-mono tabular-nums", colorClass)}>`
    - Мононширинный шрифт + tabular-nums для стабильной ширины чисел

    **ProxyNodeItem.tsx** — Элемент прокси в развёрнутом списке группы:
    - Props: `name: string`, `isActive: boolean`, `canSelect: boolean`, `delay: number | undefined`, `testing: boolean`, `onSelect: () => void`, `onTest: () => void`
    - Layout: горизонтальный flex — имя прокси слева, ProxyLatencyBadge справа
    - Если `isActive` — выделение: `bg-primary/10 border-l-2 border-primary` (цветовой акцент per user decision)
    - Если `canSelect` — cursor-pointer, hover state. При клике — вызов onSelect
    - Если НЕ canSelect — cursor-default, без hover (read-only для автогрупп)
    - Кнопка теста задержки (иконка Zap из lucide) справа от бейджа задержки, при клике — onTest
    - Компактный: высота ~32-36px, padding minimal

    **ProxyGroupCard.tsx** — Карточка группы (свёрнутая + развёрнутая):
    - Props: `groupName: string`, `density: 'min' | 'mid' | 'max'`, `typeStyle: 'badge' | 'border' | 'icon'`, `showAutoInfo: boolean`, `sortBy: 'name' | 'delay' | 'default'`
    - Данные из stores: `useProxiesStore(s => s.proxyMap[groupName])` для получения группы, `useProxiesStore` для actions
    - Данные из settings НЕ читать здесь — получает через props от ProxiesPage

    **Свёрнутое состояние (CardHeader):**
    - Клик по заголовку → `toggleExpand(groupName)`
    - Тип группы — визуальное отличие по typeStyle prop:
      - `'badge'`: shadcn Badge с текстом типа (Selector, URLTest, Fallback, LoadBalance)
      - `'border'`: цвет левой border карточки (Selector=primary, URLTest=blue, Fallback=orange, LoadBalance=green)
      - `'icon'`: иконка из lucide рядом с именем (Selector=MousePointerClick, URLTest=Timer, Fallback=ShieldCheck, LoadBalance=Scale)
    - Плотность `'min'`: имя группы + текущий прокси (now) + тип
    - Плотность `'mid'`: + ProxyLatencyBadge для текущего now + кол-во прокси (`{all.length} proxies`)
    - Плотность `'max'`: + иконка группы (если есть icon URL — <img>, иначе скрыть) + мини-список топ-3 прокси по задержке (отсортировать delayCache, взять 3 с наименьшей ненулевой задержкой)
    - Кнопка теста группы: иконка Zap, при клике `testGroupDelay(groupName)`, disabled если группа в testingGroups
    - Chevron иконка (ChevronDown/ChevronUp) для индикации expand состояния

    **Развёрнутое состояние (Collapsible + CollapsibleContent):**
    - `<Collapsible open={isExpanded}>` где `isExpanded = expandedGroups.has(groupName)`
    - При раскрытии — вызвать автотест задержки: `useEffect` при `isExpanded === true` → для каждого прокси в группе вызвать `testProxyDelay(proxyName)` (которая сама проверит кеш и не делает запрос если свежий)
    - Внутри: `<ScrollArea className="max-h-[400px]">` с полным списком прокси
    - Сортировка списка по sortBy prop:
      - `'default'`: порядок как в `all` массиве
      - `'name'`: alphabetical по имени
      - `'delay'`: по значению delay из delayCache (ascending, timeout/undefined в конец)
    - Каждый элемент — `<ProxyNodeItem>` с:
      - `isActive = proxyName === group.now`
      - `canSelect = group.type === 'Selector'`
      - `onSelect = () => selectProxyInGroup(groupName, proxyName)`
      - `delay = delayCache[proxyName]?.delay`
      - `testing = testingProxies.has(proxyName)`
      - `onTest = () => testProxyDelay(proxyName)`
    - Для автоматических групп (URLTest, Fallback) если `showAutoInfo` — показать доп. информацию под списком: testUrl, interval (из поля proxy если есть)

    **Раскрытая карточка в гриде:**
    - Когда isExpanded — карточка получает `col-span-full` для занятия всей ширины грида
    - Transition: `transition-all duration-200` на Card для плавности

    **ВАЖНО:** Для работы с Set из Zustand store использовать паттерн `useProxiesStore(s => s.expandedGroups)` и проверять через `.has()`. При рендере списка помнить что Set не serializable — если нужно, конвертировать в Array для iteration.
  </action>
  <verify>
    - `npx tsc --noEmit` — нет ошибок типов
    - Файлы существуют: ProxyLatencyBadge.tsx, ProxyNodeItem.tsx, ProxyGroupCard.tsx
    - ProxyGroupCard рендерит Collapsible с CollapsibleContent
    - ProxyNodeItem вызывает onSelect при клике
    - ProxyLatencyBadge использует formatDelay и цветовую маркировку
  </verify>
  <done>
    Три ядровых компонента proxies готовы: бейдж задержки с цветами, элемент прокси с переключением, карточка группы с inline expand и тремя уровнями плотности.
  </done>
</task>

<task type="auto">
  <name>Task 2: ProxiesToolbar + ProxiesSettingsPopover + ProxiesPage</name>
  <files>
    src/components/proxies/ProxiesToolbar.tsx
    src/components/proxies/ProxiesSettingsPopover.tsx
    src/pages/ProxiesPage.tsx
  </files>
  <action>
    **ProxiesSettingsPopover.tsx** — Поповер настроек отображения:
    - Кнопка-триггер: иконка Settings2 (lucide), без текста, tooltip "Настройки отображения"
    - Содержимое поповера (Popover + PopoverTrigger + PopoverContent):

    Секции в поповере:
    a) **Сетка** — ToggleGroup (shadcn/ui, уже установлен) с вариантами 1, 2, 3 (иконки: LayoutList, Columns2, Columns3 из lucide)
       - Значение из `useSettingsStore(s => s.proxiesGridColumns)`
       - onChange → `setProxiesGridColumns(value)`

    b) **Плотность** — ToggleGroup с вариантами: мин / сред / макс (текстовые лейблы)
       - Значение из `useSettingsStore(s => s.proxiesDensity)`
       - onChange → `setProxiesDensity(value)`

    c) **Сортировка прокси** — ToggleGroup: Имя / Задержка / По умолчанию
       - Значение из `useSettingsStore(s => s.proxiesSort)`
       - onChange → `setProxiesSort(value)`

    d) **Стиль типов групп** — ToggleGroup: Бейдж / Рамка / Иконка
       - Значение из `useSettingsStore(s => s.proxiesTypeStyle)`
       - onChange → `setProxiesTypeStyle(value)`

    e) **Доп. информация автогрупп** — Toggle (одиночный) или checkbox
       - Значение из `useSettingsStore(s => s.proxiesShowAutoInfo)`
       - onChange → `setProxiesShowAutoInfo(value)`

    Каждая секция с label и separator между секциями. PopoverContent width: 280-320px.

    **ProxiesToolbar.tsx** — Тулбар страницы:
    - Layout: горизонтальный flex с gap, items-center
    - Элементы слева направо:
      a) **Поиск** — shadcn Input с иконкой Search (lucide) prefix. Placeholder "Поиск группы или прокси..."
         - Controlled input, состояние хранить в ProxiesPage и прокидывать через props: `searchQuery: string`, `onSearchChange: (value: string) => void`
         - Фильтрация ЛОКАЛЬНАЯ (не API) — по имени группы И по именам прокси внутри группы
      b) **Фильтр по типу** — shadcn Select (уже установлен) с вариантами: Все / Selector / URLTest / Fallback / LoadBalance
         - Controlled: `typeFilter: string`, `onTypeFilterChange: (value: string) => void`
         - Значение 'all' = показать все
      c) **Разделитель** — flex-grow spacer
      d) **Кнопка «Тестировать все»** — Button variant="outline" с иконкой Zap. Текст "Тестировать все"
         - onClick → `useProxiesStore.getState().testAllGroups()`
         - disabled если любая группа в testingGroups (проверить testingGroups.size > 0)
      e) **ProxiesSettingsPopover** — встроить как компонент

    **ProxiesPage.tsx** — Полная замена placeholder:
    - Локальный state: `searchQuery` (string), `typeFilter` (string, default 'all')
    - При mount: `useEffect(() => { useProxiesStore.getState().fetchAllProxies() }, [])`
    - Получить из stores:
      - `groupNames, proxyMap, loading` из useProxiesStore
      - `proxiesGridColumns, proxiesDensity, proxiesSort, proxiesTypeStyle, proxiesShowAutoInfo` из useSettingsStore

    **Фильтрация groupNames:**
    - По typeFilter: если не 'all', оставить группы где `proxyMap[name].type === typeFilter`
    - По searchQuery: оставить группы где:
      - Имя группы содержит searchQuery (case-insensitive) ИЛИ
      - Любой прокси в group.all содержит searchQuery (case-insensitive)

    **Layout:**
    - `<div className="flex flex-col gap-4 p-4">`
    - `<ProxiesToolbar ... />` — сверху
    - Grid контейнер: `<div className={cn("grid gap-4", gridColsClass)}>` где gridColsClass:
      - 1 → `grid-cols-1`
      - 2 → `grid-cols-1 md:grid-cols-2`
      - 3 → `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - Для каждого filteredGroupName → `<ProxyGroupCard key={name} groupName={name} density={...} typeStyle={...} showAutoInfo={...} sortBy={...} />`

    **Loading state:** Если loading — показать grid из 6 Skeleton cards (высота ~100px каждый)

    **Empty state:** Если filteredGroups.length === 0 и не loading — показать текст "Группы не найдены" с иконкой Search

    **НЕ делать:**
    - Не добавлять drag-and-drop (Phase 7)
    - Не добавлять категории/списки (deferred)
    - Не добавлять polling/auto-refresh (fetch once + refresh on action)
  </action>
  <verify>
    - `npx tsc --noEmit` — нет ошибок типов
    - `npm run build` — успешная сборка
    - ProxiesPage.tsx НЕ содержит PlaceholderPage
    - ProxiesPage рендерит ProxiesToolbar и grid с ProxyGroupCard
    - ProxiesSettingsPopover содержит 5 секций настроек
    - Поиск фильтрует по имени группы и имени прокси
  </verify>
  <done>
    Страница Proxies полностью функциональна: тулбар с поиском/фильтром/настройками, grid карточек групп с настраиваемой сеткой, inline expand с полным списком прокси, переключение прокси в Selector группах, тест задержки отдельных прокси и групп, массовый тест всех групп, цветовая маркировка задержки, три уровня плотности информации.
  </done>
</task>

</tasks>

<verification>
- `npm run build` — проект собирается без ошибок
- Страница /proxies отображает proxy-groups как карточки (PROX-01)
- Клик по прокси в Selector группе переключает активный прокси с тостом (PROX-02)
- Тест задержки работает: кнопка на прокси, кнопка на группе, кнопка "Тестировать все" (PROX-03)
- Настройки отображения (сетка, плотность, сортировка, стиль типов) работают через поповер
- Поиск фильтрует по имени группы и прокси
- Фильтр по типу группы работает
- Для автогрупп (URLTest, Fallback) переключение недоступно, текущий выбор read-only
</verification>

<success_criteria>
1. Proxy-groups отображаются как карточки с настраиваемой сеткой и плотностью (PROX-01)
2. Клик по прокси в Selector группе мгновенно переключает + тост (PROX-02)
3. Тест задержки с кешированием 15 сек и цветовой маркировкой (PROX-03)
4. Поиск и фильтрация работают
5. Настройки сохраняются между сессиями
6. Проект собирается без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/03-proxies-page/03-02-SUMMARY.md`
</output>
