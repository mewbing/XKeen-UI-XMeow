---
phase: 02-overview-service-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/overview/MetricsCards.tsx
  - src/components/overview/TrafficChart.tsx
  - src/components/layout/AppLayout.tsx
  - src/stores/overview.ts
  - src/pages/OverviewPage.tsx
autonomous: true
requirements: [OVER-01, OVER-05]
gap_closure: true

must_haves:
  truths:
    - "Metric cards stay within container bounds in both compact and panels mode"
    - "Uptime counter does not reset when navigating away and back to Overview"
    - "Traffic chart shows visible colored lines for upload and download speed"
  artifacts:
    - path: "src/components/overview/MetricsCards.tsx"
      provides: "Overflow-safe metric cards in both display modes"
      contains: "overflow-hidden"
    - path: "src/components/overview/TrafficChart.tsx"
      provides: "Visible chart lines with valid CSS colors"
      contains: "var(--chart-"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Content area with overflow containment, no forced remount"
    - path: "src/stores/overview.ts"
      provides: "Persistent startTime that survives remounts"
    - path: "src/pages/OverviewPage.tsx"
      provides: "Conditional startTime initialization (only if null)"
  key_links:
    - from: "src/components/overview/TrafficChart.tsx"
      to: "src/index.css"
      via: "CSS variable usage without hsl() wrapper"
      pattern: "var\\(--chart-"
    - from: "src/pages/OverviewPage.tsx"
      to: "src/stores/overview.ts"
      via: "startTime guard prevents reset"
      pattern: "startTime.*null"
---

<objective>
Fix three UAT failures on the Overview page: (1) metric cards overflowing their containers in both compact and panels mode, (2) uptime counter resetting to 0 on every navigation, (3) traffic chart showing empty lines due to invalid CSS color syntax.

Purpose: Close gaps from UAT tests 1, 2, and 3 so the Overview page displays correctly.
Output: Working metric cards, persistent uptime, visible chart lines.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-overview-service-management/02-02-SUMMARY.md
@.planning/debug/overview-metrics-overflow-uptime.md
@.planning/debug/traffic-chart-empty.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix layout overflow at all levels and persist uptime</name>
  <files>
    src/components/overview/MetricsCards.tsx
    src/components/layout/AppLayout.tsx
    src/stores/overview.ts
    src/pages/OverviewPage.tsx
  </files>
  <action>
**MetricsCards.tsx -- fix overflow in both modes:**

1. CompactCard (line 30): Change `className="py-3 flex-1 min-w-[120px]"` to `className="py-3 flex-1 min-w-[120px] max-w-full overflow-hidden"` -- constrains growth and clips content.
2. CompactMetrics container (line 54): Change `className="flex flex-wrap gap-2"` to `className="flex flex-wrap gap-2 overflow-hidden"`.
3. PanelCard (line 96): Change `className="py-4"` to `className="py-4 overflow-hidden"`.
4. PanelsMetrics grid (line 118): Change `className="grid grid-cols-2 gap-3"` to `className="grid grid-cols-2 gap-3 overflow-hidden"`.
5. PanelCard large text: For the `text-2xl font-bold` elements on lines 146 and 150, add `truncate` class to prevent text overflow in grid cells.

**AppLayout.tsx -- fix content overflow and remove forced remount:**

1. Remove `key={location.pathname}` from the inner div (line 19). This forces full unmount/remount on navigation, causing uptime reset. The page-enter animation is NOT worth the cost of losing all component state. If animation is desired, use a CSS transition that doesn't destroy the component tree.
2. On `SidebarInset`, no direct modification needed (it's a shadcn component). Instead, add `min-w-0 overflow-hidden` to the outer content div (the one currently `className="flex flex-1 flex-col"` on line 17). This becomes `className="flex flex-1 flex-col min-w-0 overflow-hidden"`.
3. The inner animated div (previously with key): change to `className="flex flex-1 flex-col min-w-0"` (keep animation classes if reduceMotion logic is present, just add min-w-0).

**overview.ts -- guard startTime from being overwritten:**

Change `setStartTime` action to only set if current startTime is null:
```typescript
setStartTime: (time) => set((state) =>
  state.startTime === null ? { startTime: time } : {}
),
```
This prevents overwriting an already-set startTime on remount.

**OverviewPage.tsx -- no change needed to the setStartTime call itself**, because the store guard handles it. The `setStartTime(Date.now())` on mount is fine -- it only takes effect when startTime is null (first visit or after store reset).
  </action>
  <verify>
Run `pnpm run build` -- must pass with no errors. Visually confirm in the generated code:
1. MetricsCards.tsx has `overflow-hidden` on CompactCard, CompactMetrics, PanelCard, and PanelsMetrics grid.
2. AppLayout.tsx has NO `key={location.pathname}`, has `min-w-0 overflow-hidden` on content wrapper.
3. overview.ts `setStartTime` checks `state.startTime === null`.
  </verify>
  <done>
Metric cards have overflow containment at all levels. Navigation does not force remount. Uptime startTime is set only once per session (persists across page navigation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix traffic chart line colors (oklch vs hsl)</name>
  <files>
    src/components/overview/TrafficChart.tsx
  </files>
  <action>
**TrafficChart.tsx -- fix stroke colors:**

The CSS variables `--chart-1` and `--chart-2` contain full oklch() color values (Tailwind v4 convention). The current code wraps them in `hsl()` creating invalid CSS like `hsl(oklch(...))`.

1. Line 47: Change `stroke="hsl(var(--chart-1))"` to `stroke="var(--chart-1)"` -- use the CSS variable directly, it already contains a complete color value.
2. Line 56: Change `stroke="hsl(var(--chart-2))"` to `stroke="var(--chart-2)"` -- same fix.

Do NOT change the CSS variables in index.css -- they are correctly defined as full oklch() values per Tailwind v4 convention.

**Also fix the Tooltip formatter if it uses hsl() for color matching** -- check if any other place in TrafficChart.tsx uses `hsl(var(--chart-*))` pattern and apply the same fix.
  </action>
  <verify>
Run `pnpm run build` -- must pass. Grep for `hsl(var(--chart` in TrafficChart.tsx -- must return zero results. Verify `var(--chart-1)` and `var(--chart-2)` are used directly as stroke values.
  </verify>
  <done>
Traffic chart lines use valid CSS color values (direct oklch via CSS variable), making upload and download speed lines visible in the chart.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run build` passes with zero errors
2. No `hsl(var(--chart` patterns remain in any source file
3. No `key={location.pathname}` in AppLayout.tsx
4. `overflow-hidden` present on MetricsCards containers
5. `setStartTime` in overview.ts has null-guard
</verification>

<success_criteria>
- Metric cards do not overflow their parent containers in compact or panels mode
- Uptime counter survives navigation to other pages and back
- Traffic chart shows visible colored lines for upload/download speed
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-overview-service-management/02-04-SUMMARY.md`
</output>
