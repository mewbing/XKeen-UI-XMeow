---
phase: 01-scaffold-config-api-setup-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/AppSidebar.tsx
  - src/components/layout/Header.tsx
  - src/components/ui/sidebar.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/pages/OverviewPage.tsx
  - src/pages/ProxiesPage.tsx
  - src/pages/ConnectionsPage.tsx
  - src/pages/LogsPage.tsx
  - src/pages/ConfigEditorPage.tsx
  - src/pages/RulesPage.tsx
  - src/pages/GroupsPage.tsx
  - src/pages/ProvidersPage.tsx
  - src/pages/GeodataPage.tsx
  - src/pages/UpdatesPage.tsx
  - src/pages/SettingsPage.tsx
  - src/pages/PlaceholderPage.tsx
  - src/stores/settings.ts
autonomous: true
requirements:
  - SETUP-04

must_haves:
  truths:
    - "Sidebar отображается слева с 11 пунктами меню и иконками"
    - "Sidebar сворачивается до иконок и разворачивается по клику"
    - "Клик на пункт меню переводит на соответствующую страницу"
    - "Нереализованные страницы показывают заглушку с иконкой и описанием"
    - "Настройки стартовой страницы сохраняются в localStorage через Zustand persist"
    - "При перезагрузке страницы настройки восстанавливаются из localStorage"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Main layout with SidebarProvider + Sidebar + content area"
      min_lines: 20
    - path: "src/components/layout/AppSidebar.tsx"
      provides: "Sidebar with 11 menu items, Lucide icons, collapsible mode"
      min_lines: 40
    - path: "src/pages/PlaceholderPage.tsx"
      provides: "Reusable stub page with icon, title, and description"
      exports: ["PlaceholderPage"]
    - path: "src/stores/settings.ts"
      provides: "Zustand persist store for settings (isConfigured, API urls, startPage, lastVisitedPage)"
      exports: ["useSettingsStore"]
    - path: "src/App.tsx"
      provides: "Root component with BrowserRouter, routes for all 11 pages"
      contains: "BrowserRouter"
    - path: "src/pages/SettingsPage.tsx"
      provides: "Settings page with start page selection"
      min_lines: 30
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppLayout.tsx"
      via: "wraps all routes"
      pattern: "AppLayout"
    - from: "src/components/layout/AppSidebar.tsx"
      to: "react-router"
      via: "NavLink for active state"
      pattern: "NavLink|useNavigate|useLocation"
    - from: "src/stores/settings.ts"
      to: "localStorage"
      via: "Zustand persist middleware"
      pattern: "persist.*mihomo-dashboard-settings"
    - from: "src/App.tsx"
      to: "src/stores/settings.ts"
      via: "wizard gate check (isConfigured)"
      pattern: "isConfigured"
---

<objective>
Создать layout приложения: collapsible sidebar с 11 пунктами меню, routing для всех страниц, заглушки для нереализованных страниц, Zustand settings store с persist, страницу настроек.

Purpose: Layout и навигация -- каркас для всех последующих фаз. Каждая следующая фаза заменяет заглушку реальной страницей.
Output: Полноценная навигация с sidebar, 11 страниц-заглушек, settings store, страница настроек.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold-config-api-setup-wizard/01-CONTEXT.md
@.planning/phases/01-scaffold-config-api-setup-wizard/01-RESEARCH.md
@.planning/phases/01-scaffold-config-api-setup-wizard/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui компоненты + Sidebar layout + Routing</name>
  <files>
    src/components/ui/sidebar.tsx
    src/components/ui/separator.tsx
    src/components/ui/tooltip.tsx
    src/components/ui/sheet.tsx
    src/components/ui/button.tsx
    src/components/ui/input.tsx
    src/components/ui/card.tsx
    src/components/layout/AppLayout.tsx
    src/components/layout/AppSidebar.tsx
    src/components/layout/Header.tsx
    src/pages/PlaceholderPage.tsx
    src/pages/OverviewPage.tsx
    src/pages/ProxiesPage.tsx
    src/pages/ConnectionsPage.tsx
    src/pages/LogsPage.tsx
    src/pages/ConfigEditorPage.tsx
    src/pages/RulesPage.tsx
    src/pages/GroupsPage.tsx
    src/pages/ProvidersPage.tsx
    src/pages/GeodataPage.tsx
    src/pages/UpdatesPage.tsx
    src/pages/SettingsPage.tsx
    src/App.tsx
  </files>
  <action>
    1. Установить shadcn/ui компоненты через CLI:
       ```bash
       pnpm dlx shadcn@latest add sidebar button card input separator tooltip sheet
       ```
       Это создаст файлы в src/components/ui/.

    2. Создать src/pages/PlaceholderPage.tsx -- переиспользуемый компонент заглушки:
       - Props: title (string), description (string), icon (LucideIcon), phase (string)
       - Визуал: по центру страницы иконка (большая, приглушённая), заголовок, описание будущего функционала, бейдж с номером фазы
       - Использовать shadcn/ui Card для обёртки
       - Современный дизайн 2025-2026: мягкие тени, rounded-xl, приглушённые цвета

    3. Создать 10 страниц-заглушек (все кроме Settings), каждая использует PlaceholderPage:
       - OverviewPage.tsx -- "Обзор", "Мониторинг системы, трафик и управление сервисом", Home icon, Phase 2
       - ProxiesPage.tsx -- "Прокси", "Управление прокси-группами и тестирование задержки", Globe icon, Phase 3
       - ConnectionsPage.tsx -- "Подключения", "Мониторинг активных подключений в реальном времени", Link2 icon, Phase 4
       - LogsPage.tsx -- "Логи", "Просмотр логов mihomo в реальном времени", ScrollText icon, Phase 4
       - ConfigEditorPage.tsx -- "Редактор конфига", "YAML-редактор с подсветкой синтаксиса", FileCode icon, Phase 5
       - RulesPage.tsx -- "Правила", "Визуальное редактирование правил с drag-and-drop", Layers icon, Phase 6
       - GroupsPage.tsx -- "Группы", "Управление proxy-groups с перетаскиванием", Users icon, Phase 7
       - ProvidersPage.tsx -- "Провайдеры", "Мониторинг rule-providers и proxy-providers", Database icon, Phase 8
       - GeodataPage.tsx -- "Геоданные", "Просмотр GeoSite/GeoIP файлов", Map icon, Phase 9
       - UpdatesPage.tsx -- "Обновления", "Управление обновлениями дашборда", Download icon, Phase 10

    4. Создать src/components/layout/AppSidebar.tsx:
       - Использовать shadcn/ui Sidebar с collapsible="icon"
       - 11 пунктов меню с Lucide иконками (Home, Globe, Link2, ScrollText, FileCode, Layers, Users, Database, Map, Download, Settings)
       - Использовать NavLink из react-router для активного состояния (подсветка текущей страницы)
       - Русские названия пунктов меню: Обзор, Прокси, Подключения, Логи, Редактор, Правила, Группы, Провайдеры, Геоданные, Обновления, Настройки
       - В footer sidebar -- кнопка сворачивания (SidebarTrigger)
       - Современный дизайн: subtle hover effects, active indicator

    5. Создать src/components/layout/Header.tsx:
       - Верхняя панель справа от sidebar
       - SidebarTrigger (кнопка toggle sidebar)
       - Заголовок текущей страницы (определяется из route)
       - Минималистичный дизайн

    6. Создать src/components/layout/AppLayout.tsx:
       - SidebarProvider обёртка
       - Sidebar слева (AppSidebar)
       - Header сверху
       - Content area для children (Outlet из react-router)

    7. Обновить src/App.tsx:
       - BrowserRouter с Routes
       - AppLayout как обёртка для всех маршрутов
       - Route для каждой из 11 страниц:
         /overview, /proxies, /connections, /logs, /config-editor, /rules, /groups, /providers, /geodata, /updates, /settings
       - Navigate "/" -> startPage из settings store (по умолчанию /overview)
       - Wizard gate: if (!isConfigured) показать wizard placeholder (пока просто текст "Setup Wizard -- Plan 04")
       - Zustand hydration check: не показывать ничего пока store не гидрируется из localStorage (иначе мелькание wizard)

    Все импорты react-router из 'react-router' (НЕ react-router-dom -- deprecated в v7).
  </action>
  <verify>
    ```bash
    pnpm run dev
    ```
    1. Открыть http://localhost:5173 -- видим sidebar слева с 11 пунктами
    2. Кликнуть на пункт -- переходит на страницу с заглушкой
    3. Sidebar сворачивается до иконок по клику на trigger
    4. Активный пункт подсвечен
    5. Каждая заглушка показывает иконку, название и описание
  </verify>
  <done>
    Sidebar с 11 пунктами отображается, навигация между страницами работает, sidebar сворачивается/разворачивается, все нереализованные страницы показывают заглушки с описанием.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zustand settings store + Settings page</name>
  <files>
    src/stores/settings.ts
    src/pages/SettingsPage.tsx
  </files>
  <action>
    1. Создать src/stores/settings.ts -- Zustand store с persist middleware:
       - Поля: isConfigured, installationType ('local'|'cdn'|null), mihomoApiUrl, mihomoSecret, configApiUrl
       - Поля навигации: startPage ('overview'|'last-visited'|string), lastVisitedPage (string)
       - Actions: setConfigured(), setStartPage(), setLastVisitedPage(), resetConfig()
       - Persist в localStorage с ключом 'mihomo-dashboard-settings'
       - Использовать createJSONStorage(() => localStorage) для явной типизации
       - Паттерн из RESEARCH.md (Pattern 2: Zustand Persist Store)

    2. Создать src/pages/SettingsPage.tsx -- реальная страница (не заглушка):
       - Секция "Подключение": отображение текущего типа установки, адресов mihomo API и Config API (read-only, информационно)
       - Секция "Стартовая страница": RadioGroup или Select с вариантами:
         - "Обзор" (overview)
         - "Последняя посещённая" (last-visited)
         - Конкретная страница (dropdown со списком)
       - Кнопка "Сбросить настройки" -- вызывает resetConfig(), перезагружает на wizard
       - Использовать shadcn/ui Card, Button, Separator для оформления
       - Русский язык для всех надписей

    3. В App.tsx -- подключить lastVisitedPage:
       - При навигации обновлять lastVisitedPage через useEffect + useLocation
       - Redirect "/" -> определяется startPage из store:
         - 'overview' -> /overview
         - 'last-visited' -> lastVisitedPage из store
         - конкретная страница -> /[page]
  </action>
  <verify>
    1. Открыть Settings, выбрать стартовую страницу "Прокси"
    2. Перезагрузить страницу -- редирект на /proxies
    3. Открыть DevTools -> Application -> localStorage -> "mihomo-dashboard-settings" -- JSON с настройками
    4. Нажать "Сбросить настройки" -- показывается wizard placeholder
  </verify>
  <done>
    Settings store сохраняет/загружает настройки из localStorage. Страница Settings позволяет выбрать стартовую страницу. Сброс настроек возвращает к wizard. Навигация запоминает lastVisitedPage.
  </done>
</task>

</tasks>

<verification>
1. Sidebar с 11 пунктами меню (русские названия) отображается слева
2. Sidebar сворачивается до иконок, разворачивается обратно
3. Клик на пункт меню -> переход на соответствующую страницу
4. 10 страниц показывают заглушки с иконкой, названием, описанием и номером фазы
5. Settings page работает: выбор стартовой страницы сохраняется в localStorage
6. Перезагрузка страницы -> redirect на выбранную стартовую страницу
7. "Сбросить настройки" возвращает к wizard placeholder
8. Нет ошибок в консоли, TypeScript компилируется без ошибок
</verification>

<success_criteria>
- 11 маршрутов работают с корректными URL-адресами
- Sidebar collapsible с иконками Lucide
- PlaceholderPage переиспользуется для 10 заглушек
- Zustand persist store работает с localStorage
- Settings page позволяет выбрать стартовую страницу
- Wizard gate блокирует доступ к приложению если !isConfigured
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-config-api-setup-wizard/01-02-SUMMARY.md`
</output>
