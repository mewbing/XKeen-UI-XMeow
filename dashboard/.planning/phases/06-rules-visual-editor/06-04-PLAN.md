---
phase: 06-rules-visual-editor
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - src/components/rules/RulesToolbar.tsx
  - src/components/rules/RulesDiffPreview.tsx
  - src/components/rules/RuleBlockList.tsx
  - src/pages/RulesPage.tsx
  - src/pages/SettingsPage.tsx
autonomous: true
requirements:
  - RULE-08
  - RULE-03

must_haves:
  truths:
    - "Save button writes modified rules to config.yaml via Config API"
    - "Apply button saves and restarts mihomo service"
    - "Apply badge counter shows number of unsaved changes"
    - "Changed blocks highlighted visually"
    - "Diff preview shown before Apply (when enabled in settings)"
    - "Reset button reverts all changes to last saved state"
    - "Ctrl+Z undoes last operation, Ctrl+Shift+Z redoes"
    - "Danger warnings fire on MATCH and exclusion block moves"
    - "Settings page has toggles for delete confirmation and diff preview"
  artifacts:
    - path: "src/components/rules/RulesDiffPreview.tsx"
      provides: "Diff preview dialog reusing DiffPreview pattern from Phase 5"
      min_lines: 40
    - path: "src/components/rules/RulesToolbar.tsx"
      provides: "Complete toolbar with Save/Apply/Reset buttons, badge counter, Undo/Redo"
      min_lines: 80
    - path: "src/pages/SettingsPage.tsx"
      provides: "Extended with rules editor settings section"
      contains: "rulesConfirmDelete"
  key_links:
    - from: "src/components/rules/RulesToolbar.tsx"
      to: "src/stores/rules-editor.ts"
      via: "serialize() for save, resetChanges() for reset, temporal undo/redo"
      pattern: "serialize|resetChanges|temporal"
    - from: "src/components/rules/RulesToolbar.tsx"
      to: "src/lib/config-api.ts"
      via: "saveConfig for writing to backend"
      pattern: "saveConfig"
    - from: "src/pages/RulesPage.tsx"
      to: "src/components/rules/DangerWarningDialog.tsx"
      via: "danger check on block reorder"
      pattern: "DangerWarningDialog|DANGER_MATCH"
---

<objective>
Complete the Save/Apply workflow with diff preview, Undo/Redo keyboard shortcuts, danger warnings integration, Reset, badge counter, and Settings page extensions.

Purpose: Users can save changes back to config.yaml, preview diffs, undo mistakes, and manage editor preferences.
Output: Fully functional rules editor with complete save/apply/undo workflow and settings integration.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rules-visual-editor/06-RESEARCH.md
@.planning/phases/06-rules-visual-editor/06-01-SUMMARY.md
@.planning/phases/06-rules-visual-editor/06-02-SUMMARY.md
@.planning/phases/06-rules-visual-editor/06-03-SUMMARY.md
@src/stores/rules-editor.ts
@src/stores/settings.ts
@src/lib/config-api.ts
@src/components/config-editor/DiffPreview.tsx
@src/pages/SettingsPage.tsx
@src/pages/ConfigEditorPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RulesDiffPreview and Toolbar Save/Apply/Reset/Undo buttons</name>
  <files>src/components/rules/RulesDiffPreview.tsx, src/components/rules/RulesToolbar.tsx</files>
  <action>
**Create `src/components/rules/RulesDiffPreview.tsx`:**
Diff preview dialog for rules Apply action. Reuse pattern from `src/components/config-editor/DiffPreview.tsx`.
- Props: `open: boolean`, `onOpenChange: (v: boolean) => void`, `original: string`, `modified: string`, `onConfirmApply: () => void`
- Uses Monaco DiffEditor in AlertDialog (same as Phase 5 DiffPreview)
- Shows diff of only the rules section (original vs modified YAML)
- Footer: "Отменить" and "Применить" buttons
- Has "Больше не показывать" checkbox in footer — on check, calls `setRulesShowDiffBeforeApply(false)` in settings store
- Language: "yaml"

**Enhance `src/components/rules/RulesToolbar.tsx`:**
Add action buttons to the right side placeholder created in Plan 02.

Right side buttons:
1. **Undo** (Undo icon) — calls `useRulesEditorStore.temporal.getState().undo()`. Disabled when `pastStates.length === 0`.
2. **Redo** (Redo icon) — calls `useRulesEditorStore.temporal.getState().redo()`. Disabled when `futureStates.length === 0`.
3. **Reset** (RotateCcw icon) — calls `useRulesEditorStore.getState().resetChanges()`. Disabled when `!dirty`. Shows AlertDialog confirmation "Отменить все изменения?"
4. **Сохранить** (Save icon) — saves rules to config.yaml:
   - Call `useRulesEditorStore.getState().serialize()` to get current YAML
   - Call `saveConfig(yaml)` from config-api
   - On success: `markSaved()`, toast.success("Конфигурация сохранена")
   - On error: toast.error with message
   - Disabled when `!dirty`
5. **Применить (N)** (Play icon) — badge showing `changeCount` when > 0:
   - If `rulesShowDiffBeforeApply` in settings → open RulesDiffPreview with original/modified YAML
   - If disabled or confirmed in diff → call `saveConfig(yaml)` then `restartMihomo()` from mihomo-api
   - On success: `markSaved()`, toast.success("Применено и перезапущено")
   - On error: toast.error
   - Disabled when `!dirty`

Manage RulesDiffPreview open state locally in RulesToolbar (open on Apply click, close on cancel/confirm).
  </action>
  <verify>
Run `npx tsc -b --noEmit` — no TypeScript errors. RulesDiffPreview renders Monaco diff. Toolbar has 5 action buttons (Undo, Redo, Reset, Save, Apply).
  </verify>
  <done>RulesDiffPreview shows YAML diff with confirm/cancel. Toolbar has Undo/Redo/Reset/Save/Apply buttons with correct disabled states and badge counter.</done>
</task>

<task type="auto">
  <name>Task 2: Danger warnings in RuleBlockList and RulesPage keyboard shortcuts/guard</name>
  <files>src/components/rules/RuleBlockList.tsx, src/pages/RulesPage.tsx</files>
  <action>
**Integrate danger warnings into `src/components/rules/RuleBlockList.tsx`:**
Modify `onDragEnd` to check for dangerous moves before applying:
- Import `DangerWarningDialog`, `DANGER_MATCH_MOVED`, `DANGER_EXCLUSIONS_MOVED` from DangerWarningDialog
- After computing oldIndex/newIndex, check:
  1. Does the moved block contain a MATCH rule AND newIndex is not the last position? → show DANGER_MATCH_MOVED warning
  2. Does the moved block name contain "ИСКЛЮЧЕНИ" (case-insensitive) AND newIndex > 0? → show DANGER_EXCLUSIONS_MOVED warning
- If danger detected: show DangerWarningDialog. On confirm → apply move. On cancel → do nothing (move not applied).
- Use local state for pending move: `{ oldIndex, newIndex, dangerType }` and dialog open state
- Warnings shown EVERY time (no "don't show again" — per user decision)

**Enhance `src/pages/RulesPage.tsx`:**
- Add keyboard shortcut listeners:
  - `Ctrl+Z` → undo (via `useRulesEditorStore.temporal.getState().undo()`)
  - `Ctrl+Shift+Z` or `Ctrl+Y` → redo
  - `Ctrl+S` → save (call same save logic as toolbar)
  - Use `useEffect` with `keydown` event listener, `e.preventDefault()` to prevent browser defaults
  - Check `e.ctrlKey || e.metaKey` for cross-platform support
- Add navigation guard: `beforeunload` event when `dirty` is true (same pattern as ConfigEditorPage)
- Highlight changed blocks: blocks with any modified/added/removed rules since last save get a colored left border (e.g. `border-l-4 border-l-amber-500`). Compare current blocks against original by tracking which block IDs have been modified in the store. Pass `changedBlockIds: Set<string>` as prop to RuleBlockList.
  </action>
  <verify>
Run `npx tsc -b --noEmit` — no TypeScript errors. Danger warning dialog appears on MATCH/exclusion moves. Ctrl+Z/Ctrl+S work. Browser warns on page leave with unsaved changes.
  </verify>
  <done>Danger warnings fire on MATCH and exclusion block moves. Keyboard shortcuts (Ctrl+Z/S) work. Navigation guard warns on unsaved changes. Changed blocks highlighted with colored border.</done>
</task>

<task type="auto">
  <name>Task 3: Settings page extensions for rules editor</name>
  <files>src/pages/SettingsPage.tsx</files>
  <action>
**Extend `src/pages/SettingsPage.tsx`:**
Add a new section "Визуальный редактор правил" to the settings page.

Add it after the "Редактор конфига" section (which has showDiffBeforeApply toggle).

Section contents:
1. **"Подтверждение удаления"** — Switch (toggle)
   - Label: "Спрашивать подтверждение при удалении правил и блоков"
   - Reads `rulesConfirmDelete` from settings, writes via `setRulesConfirmDelete`
   - Description text: "Можно также отключить через галочку в диалоге удаления"

2. **"Diff-превью при применении"** — Switch (toggle)
   - Label: "Показывать diff изменений перед применением"
   - Reads `rulesShowDiffBeforeApply`, writes via `setRulesShowDiffBeforeApply`
   - Description text: "Можно также отключить через галочку в диалоге превью"

3. **"Группировка по умолчанию"** — Select dropdown
   - Options: "По proxy-группам", "По секциям конфига", "Двухуровневая"
   - Reads/writes `rulesGrouping`

4. **"Лейаут по умолчанию"** — Select dropdown
   - Options: "Вертикальный список", "Сетка карточек", "Как на Proxies"
   - Reads/writes `rulesLayout`

5. **"Плотность карточек"** — Select dropdown
   - Options: "Минимальная", "Подробная"
   - Reads/writes `rulesDensity`

6. **"Создание нового блока"** — Select dropdown
   - Options: "Диалог", "Инлайн-карточка"
   - Reads/writes `rulesNewBlockMode`

Follow the exact same pattern as existing settings sections: Label component with htmlFor, Switch/Select with value binding and onChange handler, muted description text below.

Use the Switch component (already imported in SettingsPage from `@/components/ui/switch`).
  </action>
  <verify>
Run `npx tsc -b --noEmit` — no TypeScript errors. Settings page renders new "Визуальный редактор правил" section with all 6 controls.
  </verify>
  <done>Settings page has complete rules editor section with 2 switches (confirm delete, diff preview) and 4 selects (grouping, layout, density, new block mode). All toggles read/write from settings store correctly.</done>
</task>

</tasks>

<verification>
1. `npx tsc -b --noEmit` passes
2. Save button: writes rules YAML to config API, shows success toast
3. Apply button: shows diff preview (when enabled), saves and restarts mihomo
4. Apply badge: shows change count (e.g. "Применить (3)")
5. Reset: reverts to last saved state
6. Ctrl+Z: undoes last operation
7. Ctrl+Shift+Z: redoes last undone operation
8. Danger warning: shown when MATCH moved from last position
9. Danger warning: shown when exclusions block moved from first position
10. Changed blocks: highlighted with colored border
11. Navigation guard: warns on page leave with unsaved changes
12. Settings: 6 rules editor controls in Settings page
</verification>

<success_criteria>
Complete rules editor with Save/Apply/Reset/Undo/Redo workflow, diff preview, danger warnings, highlighted changes, keyboard shortcuts, navigation guard, and full Settings integration. All RULE-01 through RULE-08 requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rules-visual-editor/06-04-SUMMARY.md`
</output>
