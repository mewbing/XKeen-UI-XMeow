---
phase: 12-go-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - cmd/antigravity/main.go
  - internal/config/config.go
  - internal/server/server.go
  - internal/server/routes.go
  - internal/server/middleware.go
  - internal/spa/embed.go
  - internal/handler/health.go
  - internal/handler/helpers.go
  - internal/backup/backup.go
autonomous: true
requirements:
  - GOBK-03
  - GOBK-07
  - GOBK-08
must_haves:
  truths:
    - "Go binary starts on :5000 and serves SPA from embedded filesystem"
    - "SPA client-side routing works (any non-API path returns index.html)"
    - "CORS headers present in dev mode, absent in production"
    - "Config paths read from environment variables with correct defaults"
    - "GET /api/health returns {status: ok}"
    - "Auth middleware validates X-API-Key on protected routes"
  artifacts:
    - path: "go.mod"
      provides: "Go module definition"
      contains: "github.com/mewbing/XKeen-UI-Xmeow"
    - path: "cmd/antigravity/main.go"
      provides: "Application entry point with graceful shutdown"
      min_lines: 40
    - path: "internal/config/config.go"
      provides: "AppConfig struct with env var loading"
      exports: ["AppConfig", "LoadConfig"]
    - path: "internal/server/server.go"
      provides: "HTTP server setup"
      min_lines: 20
    - path: "internal/server/routes.go"
      provides: "Chi router with all route registrations"
      min_lines: 30
    - path: "internal/spa/embed.go"
      provides: "SPA handler with index.html fallback"
      min_lines: 25
    - path: "internal/handler/health.go"
      provides: "Health check handler"
    - path: "internal/handler/helpers.go"
      provides: "writeJSON helper, readJSONBody helper"
    - path: "internal/backup/backup.go"
      provides: "Timestamped backup creation utility"
  key_links:
    - from: "cmd/antigravity/main.go"
      to: "internal/server/server.go"
      via: "server.New(cfg) creates HTTP server"
      pattern: "server\\.New"
    - from: "internal/server/routes.go"
      to: "internal/spa/embed.go"
      via: "r.NotFound(spaHandler) for SPA fallback"
      pattern: "NotFound.*spa"
    - from: "internal/config/config.go"
      to: "cmd/antigravity/main.go"
      via: "LoadConfig() called at startup"
      pattern: "config\\.LoadConfig"
---

<objective>
Go project scaffold: module init, chi router, SPA embed, CORS middleware, auth middleware, config loading, health endpoint, graceful shutdown.

Purpose: Foundation that all subsequent plans build on. Go binary starts, serves SPA, handles CORS, validates auth, reads config from env vars.
Output: Compilable Go binary that serves embedded SPA on :5000 with health check endpoint.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-go-backend-core/12-CONTEXT.md
@.planning/phases/12-go-backend-core/12-RESEARCH.md
@backend/server.py
@vite.config.ts

<interfaces>
<!-- Flask backend reference for env vars and defaults -->
From backend/server.py:
```python
MIHOMO_CONFIG_PATH = os.environ.get('MIHOMO_CONFIG_PATH', '/opt/etc/mihomo/config.yaml')
XKEEN_DIR = os.environ.get('XKEEN_DIR', '/opt/etc/xkeen')
BACKUP_DIR = os.environ.get('BACKUP_DIR', '/opt/etc/mihomo/backups')
XKEEN_BIN = os.environ.get('XKEEN_BIN', '/opt/sbin/xkeen')
XKEEN_LOG_DIR = os.environ.get('XKEEN_LOG_DIR', '/opt/var/log/xray')
ALLOWED_LOGS = {'error': 'error.log', 'access': 'access.log'}
XKEEN_FILES = {
    'ip_exclude': 'ip_exclude.lst',
    'port_exclude': 'port_exclude.lst',
    'port_proxying': 'port_proxying.lst',
}
```

From src/lib/config-api.ts:
```typescript
// Frontend uses configApiUrl from settings store for all /api/* calls
function getBaseUrl(): string {
  return useSettingsStore.getState().configApiUrl
}
```

From src/hooks/useLogWebSocket.ts:
```typescript
// Frontend constructs WS URL from configApiUrl
function getWsUrl(): string {
  const base = useSettingsStore.getState().configApiUrl
  return base.replace(/^http/, 'ws') + '/ws/logs'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go module init, dependencies, config, backup utility</name>
  <files>go.mod, go.sum, internal/config/config.go, internal/backup/backup.go, internal/handler/helpers.go</files>
  <action>
1. Initialize Go module in dashboard/ root:
   ```
   go mod init github.com/mewbing/XKeen-UI-Xmeow
   ```

2. Install dependencies (use chi v5 per Claude's Discretion -- zero deps, net/http compatible, NotFound handler for SPA):
   ```
   go get github.com/go-chi/chi/v5@v5.2.5
   go get github.com/gorilla/websocket@v1.5.3
   go get github.com/goccy/go-yaml@latest
   go get github.com/fsnotify/fsnotify@v1.9.0
   go get github.com/go-chi/cors@latest
   ```

3. Create `internal/config/config.go`:
   - `AppConfig` struct with fields: MihomoConfigPath, XkeenDir, BackupDir, XkeenBin, XkeenLogDir, Port (int), DevMode (bool), Version (string)
   - Add `AllowedLogs` map: `{"error": "error.log", "access": "access.log"}`
   - Add `XkeenFiles` map: `{"ip_exclude": "ip_exclude.lst", "port_exclude": "port_exclude.lst", "port_proxying": "port_proxying.lst"}`
   - `LoadConfig() *AppConfig` reads from env vars with IDENTICAL defaults to Flask:
     - MIHOMO_CONFIG_PATH -> `/opt/etc/mihomo/config.yaml`
     - XKEEN_DIR -> `/opt/etc/xkeen`
     - BACKUP_DIR -> `/opt/etc/mihomo/backups`
     - XKEEN_BIN -> `/opt/sbin/xkeen`
     - XKEEN_LOG_DIR -> `/opt/var/log/xray`
     - PORT -> `5000`
     - DEV_MODE -> `""` (empty = false)
     - VERSION -> `dev` (overridden by ldflags in CI)
   - `GetMihomoSecret(configPath string) string` -- reads mihomo config.yaml, extracts `secret` field value. Returns empty string if not found.
   - `GetMihomoExternalController(configPath string) string` -- reads `external-controller` field. Returns `127.0.0.1:9090` default.
   - Helper: `getEnv(key, fallback string) string`, `getEnvInt(key string, fallback int) int`

4. Create `internal/backup/backup.go`:
   - `CreateBackup(sourcePath, backupName, extension, backupDir string) (string, error)`
   - If source file does not exist, return ("", nil) -- no error, no backup
   - MkdirAll backupDir, read source, write to `{backupDir}/{backupName}_{YYYYMMDD_HHMMSS}{extension}`
   - Timestamp format: `time.Now().Format("20060102_150405")` -- identical to Flask

5. Create `internal/handler/helpers.go`:
   - `writeJSON(w http.ResponseWriter, status int, data interface{})` -- sets Content-Type application/json, writes status code, encodes data
   - `readJSONBody(r *http.Request, v interface{}) error` -- decodes request body into v, returns error if malformed
   - These helpers used by ALL handler files in subsequent plans
  </action>
  <verify>
    <automated>cd d:/AI/antigravity/claude/config/mihomo/dashboard && go build ./internal/config/ && go build ./internal/backup/ && go build ./internal/handler/ && echo "All packages compile"</automated>
  </verify>
  <done>go.mod exists with all 5 dependencies, config/backup/handler packages compile, env var defaults match Flask exactly</done>
</task>

<task type="auto">
  <name>Task 2: Chi router, SPA embed, CORS, auth middleware, health endpoint, main.go with graceful shutdown</name>
  <files>cmd/antigravity/main.go, internal/server/server.go, internal/server/routes.go, internal/server/middleware.go, internal/spa/embed.go, internal/handler/health.go</files>
  <action>
1. Create `internal/spa/embed.go`:
   - `//go:embed dist/*` directive on `var DistFS embed.FS` (package-level)
   - IMPORTANT: The embed directive will be in the spa package. Since dist/ is at dashboard root and embed paths are relative to the source file, create a build constraint: use `//go:embed all:dist` ONLY if the file is at root level. Since internal/spa/ is a subdirectory, the embed directive WON'T work directly. SOLUTION: Put the embed var in cmd/antigravity/main.go (which is also not root) -- actually, the `//go:embed` directive path is relative to the file's directory. So place embed declaration in a file at dashboard root level: create `embed.go` at `dashboard/embed.go` package main won't work either.
   - CORRECT APPROACH: Create `internal/spa/spa.go` with `func NewSPAHandler(fsys fs.FS) http.Handler` that accepts an fs.FS parameter (the embedded dist sub-filesystem). The actual `//go:embed dist/*` lives in `cmd/antigravity/main.go` since that's where we control the embed path relative to dashboard root. BUT cmd/antigravity/ is 2 dirs deep -- embed paths are relative to the Go source file containing the directive.
   - FINAL APPROACH: Create `spa.go` at project root (`dashboard/spa.go`) in a separate package won't work in cmd. Instead: put embed in root-level file `dashboard/embed.go` with `package main` -- but that conflicts with cmd/antigravity being main.
   - SIMPLEST CORRECT APPROACH: Place `//go:embed dist/*` in `cmd/antigravity/main.go` and pass `distFS` to spa handler. The embed path is relative to the source file, so in cmd/antigravity/ the path would be `../../dist/*`. BUT Go embed does NOT allow `..` in paths.
   - ACTUAL SOLUTION: Create `embed.go` at dashboard root directory with its own package name. Since go.mod is at dashboard root, create file `embed.go` in root:
     ```go
     package dashboard // or use a simple package name
     //go:embed dist/*
     var DistFS embed.FS
     ```
     But then cmd/antigravity/main.go imports it. This is clean.
   - CORRECTION: The module is `github.com/mewbing/XKeen-UI-Xmeow`. A root-level `embed.go` would be package `xkeenui` (or similar). Then import as `github.com/mewbing/XKeen-UI-Xmeow` in main.go. This is the standard Go pattern for embedding at module root.
   - Create `embed.go` at dashboard root: package name `xkeenui`, contains `//go:embed all:dist/*` and `var DistFS embed.FS`
   - Create `internal/spa/spa.go`: `func NewSPAHandler(distFS fs.FS) http.Handler` that:
     a. Uses `fs.Sub(distFS, "dist")` to strip the dist/ prefix
     b. Creates `http.FileServer(http.FS(sub))`
     c. Pre-reads index.html content at init time for SPA fallback
     d. On request: check if file exists via `fs.Stat(sub, path)` -- if yes, serve file; if no, serve index.html with Content-Type text/html and Cache-Control no-cache
     e. For "/" path, serve index.html directly

2. Create `internal/server/middleware.go`:
   - `AuthMiddleware(getSecret func() string) func(http.Handler) http.Handler` -- chi-compatible middleware
   - Checks `Authorization: Bearer {token}` header OR `X-API-Key: {token}` header
   - Compares against secret from getSecret() (which reads mihomo config.yaml secret field)
   - If secret is empty (not configured), skip auth (allow all) -- per RESEARCH open question #4
   - If secret mismatch, return 401 `{"error": "Unauthorized"}`
   - Skip auth for OPTIONS requests (CORS preflight)

3. Create `internal/server/routes.go`:
   - `func NewRouter(cfg *config.AppConfig, spaHandler http.Handler) *chi.Mux`
   - Global middleware: chi middleware.Logger, middleware.Recoverer, middleware.RealIP
   - CORS middleware (dev mode only, per GOBK-07): AllowedOrigins ["*"], AllowedMethods ["GET","PUT","POST","DELETE","OPTIONS"], AllowedHeaders ["Accept","Authorization","Content-Type","X-API-Key"], AllowCredentials true
   - Auth middleware on /api/* group (except /api/health)
   - Route group `/api` with all endpoint placeholders (only /api/health implemented now; rest will be added in plans 02-04). Add comment placeholder stubs for the remaining 14 endpoints.
   - WebSocket route `/ws/logs` placeholder (plan 03)
   - Mihomo reverse proxy `/api/mihomo/*` placeholder (plan 04)
   - `r.NotFound(spaHandler.ServeHTTP)` as LAST -- SPA fallback

4. Create `internal/handler/health.go`:
   - `func Health(w http.ResponseWriter, r *http.Request)` -- returns `{"status": "ok"}` with 200

5. Create `internal/server/server.go`:
   - `type Server struct` with http.Server, config, router
   - `func New(cfg *config.AppConfig, distFS fs.FS) *Server` -- creates chi router, SPA handler, wires everything
   - `func (s *Server) Start() error` -- ListenAndServe
   - `func (s *Server) Shutdown(ctx context.Context) error` -- graceful shutdown

6. Create `cmd/antigravity/main.go`:
   - Import root package for DistFS: `xkeenui "github.com/mewbing/XKeen-UI-Xmeow"`
   - `config.LoadConfig()`
   - `server.New(cfg, xkeenui.DistFS)`
   - Graceful shutdown with signal.NotifyContext (SIGINT, SIGTERM)
   - 5-second shutdown timeout
   - Log startup message: "Antigravity Dashboard listening on :PORT"
   - ldflags version: `var Version = "dev"` passed to config

IMPORTANT: For the embedded dist/ directory -- during development dist/ may not exist. Create a minimal placeholder: `dist/index.html` with basic HTML that says "Run npm run build to generate SPA". This allows `go build` to succeed. Add `dist/` to .gitignore note but don't modify .gitignore.

For embed to work at root level, the root package cannot be `main`. Use package name matching the last element of the module path. Since module is `github.com/mewbing/XKeen-UI-Xmeow`, the root package name should be something importable. ACTUALLY, in Go you cannot import the root module as a package if it has package `main`. The solution is:
- Root package should NOT be `main`
- Use a descriptive package name like `xkeenui`
- cmd/antigravity/main.go imports it: `import xkeenui "github.com/mewbing/XKeen-UI-Xmeow"`

Create file `dashboard/embed.go` with:
```go
package xkeenui

import "embed"

//go:embed all:dist
var DistFS embed.FS
```

Create a minimal `dist/index.html` placeholder so embed compiles.
  </action>
  <verify>
    <automated>cd d:/AI/antigravity/claude/config/mihomo/dashboard && go build -o antigravity.exe ./cmd/antigravity/ && echo "Binary built successfully"</automated>
  </verify>
  <done>Go binary compiles successfully. Running it starts HTTP server on :5000. GET /api/health returns {"status":"ok"}. Non-API paths serve index.html from embedded FS. CORS headers appear when DEV_MODE is set.</done>
</task>

</tasks>

<verification>
1. `go build ./cmd/antigravity/` compiles without errors
2. All packages under internal/ compile: `go build ./internal/...`
3. `go vet ./...` passes with no issues
4. Binary runs and logs "Antigravity Dashboard listening on :5000"
</verification>

<success_criteria>
- Go module initialized with 5 external dependencies (chi, gorilla/websocket, goccy/yaml, fsnotify, chi/cors)
- Config loading reads all 7 env vars with Flask-identical defaults
- Chi router with SPA fallback, CORS (dev), auth middleware skeleton
- GET /api/health returns {"status": "ok"}
- Binary compiles for local platform
- Graceful shutdown on SIGINT/SIGTERM
</success_criteria>

<output>
After completion, create `.planning/phases/12-go-backend-core/12-01-SUMMARY.md`
</output>
