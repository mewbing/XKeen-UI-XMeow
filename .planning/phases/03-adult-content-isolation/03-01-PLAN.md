---
phase: 03-adult-content-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.yaml]
autonomous: false
requirements: [ADULT-01, ADULT-02, ADULT-03, ADULT-04, ADULT-05, ADULT-06]

must_haves:
  truths:
    - "Все adult proxy-groups (Sin, ST, CB, BG, BGP) находятся между маркерами # >>> ADULT / # <<< ADULT в секции proxy-groups"
    - "Все adult rule-providers (Sin_in, ST_in, BG_in, BGP_in, CB_in, category-porn, oisd_nsfw_small, oisd_nsfw_big) находятся между маркерами в секции rule-providers"
    - "Все 38 adult-правил (inline + RULE-SET + из Other + NSFW) находятся в маркированном блоке в начале секции rules (после QUIC/SAFE)"
    - "Ссылки на adult-группы в GLOBAL обёрнуты маркерами"
    - "После удаления строк между маркерами, grep по adult-keywords даёт 0 совпадений"
    - "Конфиг остаётся валидным YAML"
  artifacts:
    - path: "config.yaml"
      provides: "Конфиг с маркированными adult-блоками во всех 4 секциях"
      contains: "# >>> ADULT"
  key_links:
    - from: "config.yaml (proxy-groups)"
      to: "config.yaml (rules)"
      via: "adult proxy-group names (Sin, ST, CB, BG, BGP) referenced in rules"
      pattern: "(Sin|ST|CB|BG|BGP)"
    - from: "config.yaml (rule-providers)"
      to: "config.yaml (rules)"
      via: "RULE-SET references to adult providers"
      pattern: "RULE-SET,(Sin_in|ST_in|BG_in|BGP_in|CB_in|category-porn|oisd_nsfw)"
    - from: "config.yaml (GLOBAL proxies)"
      to: "config.yaml (proxy-groups)"
      via: "GLOBAL references to adult group names"
      pattern: "- '(Sin|ST|CB|BG|BGP)'"
---

<objective>
Консолидировать весь adult-контент в config.yaml в маркированные блоки `# >>> ADULT` / `# <<< ADULT` во всех 4 секциях: proxy-groups, rule-providers, rules и GLOBAL references.

Purpose: Подготовить конфиг к автоматической генерации work-варианта (Phase 5) путем простого удаления строк между маркерами. Adult-контент должен быть полностью изолирован -- grep по adult-keywords вне маркеров = 0.

Output: config.yaml с 4 парами маркеров (8 маркерных строк), все 56 adult-элементов внутри маркеров.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adult-content-isolation/03-CONTEXT.md
@.planning/phases/03-adult-content-isolation/03-RESEARCH.md
@.planning/phases/02-service-deduplication/02-03-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@config.yaml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Подтверждение находок аудита adult-контента</name>
  <decision>Подтверждение переноса 3 дополнительных элементов в adult-блок (найдены при аудите, не перечислены в ADULT-04)</decision>
  <context>
CONTEXT.md (locked decision): "Новые находки аудита показать списком перед выполнением для подтверждения."
RESEARCH.md выявил 3 элемента, которые необходимо маркировать как adult для прохождения ADULT-06 (grep по "nsfw", "hembed" = 0):

1. **hembed.com / hembed** (строки DOMAIN-SUFFIX,hembed.com,Other и DOMAIN-KEYWORD,hembed,Other) -- видео-embed CDN для hanime1.me. Без маркировки "hembed" останется в work-конфиге.
2. **oisd_nsfw_small** (rule-provider + RULE-SET правило в rules) -- NSFW-фильтр OISD, маршрутизируется в Ad-Filter. Без маркировки "nsfw" останется в work-конфиге (имя провайдера, URL, path содержат "nsfw").
3. **oisd_nsfw_big** (rule-provider + RULE-SET правило в rules) -- NSFW-фильтр OISD, маршрутизируется в Ad-Filter. Аналогично oisd_nsfw_small.

Все остальные элементы -- однозначно adult (stripchat, chaturbate, bongacams, sinparty, pornhub, onlyfans, fansly, hanime1, e-hentai, rule34, category-porn).

НЕ включаются в adult (подтверждено research):
- patreon.com -- универсальная платформа, не adult по основной функции
- kemono.su -- пиратский агрегатор, в ru-inline-banned (заблокирован в РФ)
  </context>
  <options>
    <option id="approve-all">
      <name>Одобрить все 3 находки</name>
      <pros>Гарантирует ADULT-06 (grep = 0 для "nsfw" и "hembed"). Полная изоляция.</pros>
      <cons>oisd_nsfw фильтры не будут блокировать NSFW-рекламу в work-конфиге (minor -- основной OISD big/small остаётся)</cons>
    </option>
    <option id="approve-hembed-only">
      <name>Одобрить только hembed, оставить oisd_nsfw</name>
      <pros>NSFW-фильтрация рекламы сохранится в work-конфиге</pros>
      <cons>ADULT-06 провалится на grep "nsfw" -- придётся исключить "nsfw" из списка keywords</cons>
    </option>
    <option id="reject-all">
      <name>Отклонить все находки</name>
      <pros>Минимальные изменения</pros>
      <cons>ADULT-06 провалится на grep "nsfw" и "hembed"</cons>
    </option>
  </options>
  <resume-signal>Выберите: approve-all, approve-hembed-only, или reject-all (или укажите свой вариант)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Реструктуризация config.yaml -- перемещение и маркировка adult-контента</name>
  <files>config.yaml</files>
  <action>
Перестроить config.yaml: переместить все adult-элементы в маркированные блоки. Работа идёт в 4 секциях одного файла. Имена proxy-groups и rule-providers НЕ менять (per CONTEXT.md locked decision). Маршрутизацию правил из "Other" и "Ad-Filter" сохранить (`,Other` и `,Ad-Filter`).

**Секция 1: proxy-groups (ADULT-01)**
- Вырезать 5 proxy-groups: Sin, ST, CB, BG, BGP из текущего расположения (между группами '53' и 'NAS' или аналогичное текущее положение)
- Вставить их в конец секции proxy-groups, непосредственно ПЕРЕД группой GLOBAL
- Обернуть блоком:
  ```
  # >>> ADULT
  - name: 'Sin'
    ...
  - name: 'BGP'
    ...
  # <<< ADULT
  ```
- Внутри блока: один сплошной блок, без подгрупп и комментариев-разделителей
- Порядок групп внутри: сохранить текущий (Sin, ST, CB, BG, BGP)

**Секция 2: GLOBAL references (ADULT-05)**
- Найти в GLOBAL proxy-group (type: select, hidden: true) список proxies
- Найти ссылки на 'Sin', 'ST', 'CB', 'BG', 'BGP'
- Сгруппировать их вместе (если разбросаны) и обернуть маркерами:
  ```
  # >>> ADULT
  - 'Sin'
  - 'ST'
  - 'CB'
  - 'BG'
  - 'BGP'
  # <<< ADULT
  ```
- Проверить ВСЕ proxy-groups на перекрёстные ссылки на adult-группы (не только GLOBAL). Если найдутся -- маркировать аналогично.

**Секция 3: rule-providers (ADULT-02)**
- Вырезать 8 rule-providers из текущих расположений:
  - Sin_in, ST_in, BG_in, BGP_in, CB_in (inline-провайдеры, текущее расположение ~строки 611-660)
  - category-porn (http/mrs провайдер, текущее расположение ~строка 767-773)
  - oisd_nsfw_small, oisd_nsfw_big (http/mrs провайдеры, текущее расположение ~строки 1149-1162)
- Вставить ВСЕ 8 провайдеров в конец секции rule-providers, перед началом секции rules
- Обернуть маркерами:
  ```
  # >>> ADULT
  Sin_in:
    ...
  ...
  oisd_nsfw_big:
    ...
  # <<< ADULT
  ```
- Порядок внутри: сначала inline-провайдеры (Sin_in, ST_in, BG_in, BGP_in, CB_in), потом http-провайдеры (category-porn, oisd_nsfw_small, oisd_nsfw_big)

**Секция 4: rules (ADULT-03, ADULT-04)**
- Собрать ВСЕ 38 adult-правил из разных мест в секции rules:
  - Блок A (18 inline): DOMAIN-SUFFIX и DOMAIN-REGEX для sinparty, stripchat, bongacams, chaturbate, bongamodels
  - Блок B (5 RULE-SET): Sin_in, ST_in, BG_in, BGP_in, CB_in
  - Блок C (13 из Other): onlyfans, fansly, pornhub (3 правила), hanime1 (2), hembed (2), e-hentai (2), rule34 (1), category-porn (1) -- сохранить маршрутизацию `,Other`
  - Блок D (2 NSFW): oisd_nsfw_small, oisd_nsfw_big -- сохранить маршрутизацию `,Ad-Filter`
- Удалить эти правила из текущих расположений
- Вставить единый adult-блок В НАЧАЛЕ секции rules, ПОСЛЕ правил QUIC и SAFE (первые 2 правила: RULE-SET,quic,QUIC и AND,...,SAFE)
- Обернуть маркерами:
  ```
  # >>> ADULT
  - DOMAIN-SUFFIX,sinparty,Sin
  ... (все 38 правил)
  - RULE-SET,oisd_nsfw_big,Ad-Filter
  # <<< ADULT
  ```
- Порядок внутри adult-блока: сначала inline DOMAIN-SUFFIX (7 правил), затем DOMAIN-REGEX (11 правил), затем RULE-SET для adult-провайдеров (5 правил), затем правила из Other (13 правил, сохраняя `,Other`), затем NSFW RULE-SET (2 правила)

**ВАЖНО:**
- НЕ создавать новые proxy-groups
- НЕ переименовывать существующие группы/провайдеры
- НЕ менять маршрутизацию (`,Other` остаётся `,Other`, `,Ad-Filter` остаётся `,Ad-Filter`)
- Маркеры с пустой строкой до и после для читаемости
- Убедиться что при перемещении правил из "Other" они НЕ остаются в старом месте (cut, not copy)
  </action>
  <verify>
1. Подсчёт маркеров: `grep -c ">>> ADULT\|<<< ADULT" config.yaml` -- ожидание: 8 (4 пары)
2. Подсчёт adult proxy-groups между маркерами: grep "name: 'Sin'\|name: 'ST'\|name: 'CB'\|name: 'BG'\|name: 'BGP'" -- все 5 внутри маркеров
3. Подсчёт adult rule-providers между маркерами: Sin_in, ST_in, BG_in, BGP_in, CB_in, category-porn, oisd_nsfw_small, oisd_nsfw_big -- все 8 внутри маркеров
4. Подсчёт adult-правил между маркерами в rules: ожидание 38 правил
5. YAML валидность: `python -c "import yaml; yaml.safe_load(open('config.yaml', encoding='utf-8'))"` -- no errors
  </verify>
  <done>Все 56 adult-элементов перемещены в маркированные блоки. 4 пары маркеров (proxy-groups, rule-providers, rules, GLOBAL). Конфиг валиден.</done>
</task>

<task type="auto">
  <name>Task 3: Финальная верификация -- grep adult-keywords вне маркеров</name>
  <files>config.yaml</files>
  <action>
Выполнить финальную проверку ADULT-06: после (виртуального) удаления всего между маркерами, grep по расширенному списку adult-keywords должен дать 0 совпадений.

**Шаг 1: Проверка grep вне маркеров**
Удалить все строки между `# >>> ADULT` и `# <<< ADULT` (включительно) через sed/awk, и grep по списку:
```
pornhub|stripchat|chaturbate|bongacams|bongacam|bongamodels|onlyfans|fansly|hentai|e-hentai|rule34|nsfw|porn|sinparty|hanime|hembed
```
Ожидание: 0 совпадений.

**Шаг 2: Проверка структурной целостности**
- Все RULE-SET в rules ссылаются на существующий rule-provider (нет "висящих" ссылок)
- Все rule-providers, на которые есть RULE-SET ссылки, существуют в секции rule-providers
- Группы Sin, ST, CB, BG, BGP упоминаются ТОЛЬКО внутри маркированных блоков (кроме определений самих групп)
- GLOBAL не содержит ссылок на несуществующие группы

**Шаг 3: Проверка порядка правил**
- Adult-блок в rules идёт ПОСЛЕ QUIC/SAFE и ПЕРЕД всеми остальными правилами
- Убедиться что bongacams/pornhub/etc домены будут матчиться РАНЬШЕ чем DOMAIN-SUFFIX,ru (first-match-wins)

**Если обнаружены проблемы:** исправить config.yaml и повторить проверку.
  </action>
  <verify>
1. `sed '/# >>> ADULT/,/# <<< ADULT/d' config.yaml | grep -icE 'pornhub|stripchat|chaturbate|bongacams|bongacam|bongamodels|onlyfans|fansly|hentai|e-hentai|rule34|nsfw|porn|sinparty|hanime|hembed'` -- результат: 0
2. YAML валидность повторно: `python -c "import yaml; yaml.safe_load(open('config.yaml', encoding='utf-8'))"`
3. Санитарная проверка провайдеров: каждый RULE-SET в rules ссылается на определённый provider
  </verify>
  <done>ADULT-06 подтверждён: grep по adult-keywords вне маркеров = 0. Конфиг структурно целостен. Порядок правил корректен (adult ПЕРЕД общими правилами). Фаза 3 завершена.</done>
</task>

</tasks>

<verification>
1. Маркеры: 4 пары `# >>> ADULT` / `# <<< ADULT` (proxy-groups, rule-providers, rules, GLOBAL) -- итого 8 маркерных строк
2. ADULT-01: Sin, ST, CB, BG, BGP в маркированном блоке в конце proxy-groups (перед GLOBAL)
3. ADULT-02: Sin_in, ST_in, BG_in, BGP_in, CB_in, category-porn, oisd_nsfw_small, oisd_nsfw_big в маркированном блоке в конце rule-providers
4. ADULT-03: 18 inline-правил + 5 RULE-SET + 2 NSFW в маркированном блоке в начале rules (после QUIC/SAFE)
5. ADULT-04: OnlyFans, Fansly, PornHub, hanime1, hembed, e-hentai, rule34, category-porn перенесены из "Other" в adult-блок
6. ADULT-05: Ссылки Sin, ST, CB, BG, BGP в GLOBAL обёрнуты маркерами
7. ADULT-06: `sed '/# >>> ADULT/,/# <<< ADULT/d' config.yaml | grep -icE '...'` = 0
8. YAML validity: python yaml.safe_load без ошибок
</verification>

<success_criteria>
- 4 пары маркеров в config.yaml (8 маркерных строк)
- 56 adult-элементов внутри маркеров
- grep по adult-keywords вне маркеров = 0
- Валидный YAML
- Все ADULT-01..ADULT-06 requirements выполнены
</success_criteria>

<output>
After completion, create `.planning/phases/03-adult-content-isolation/03-01-SUMMARY.md`
</output>
