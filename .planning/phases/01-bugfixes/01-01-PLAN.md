---
phase: 01-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.yaml]
autonomous: true
requirements: [DEDUP-05, DEDUP-08, DEDUP-09]

must_haves:
  truths:
    - "DST-PORT rule for port 53 parses without errors in mihomo -t"
    - "Only oisd_big exists in rule-providers and rules -- oisd_small completely removed"
    - "bongacams.ru does not appear in Other category -- only covered by BG group rules"
  artifacts:
    - path: "config.yaml"
      provides: "Mihomo config with 3 bugfixes applied"
      contains: "AND,((DST-PORT,53)),53"
  key_links:
    - from: "config.yaml rules section"
      to: "proxy group '53'"
      via: "AND,((DST-PORT,53)),53 rule"
      pattern: "AND,\\(\\(DST-PORT,53\\)\\),53"
    - from: "config.yaml rules section"
      to: "oisd_big rule-provider"
      via: "RULE-SET reference"
      pattern: "RULE-SET,oisd_big,Ad-Filter"
    - from: "config.yaml BG inline rules (line ~1309)"
      to: "bongacams.ru domain routing"
      via: "DOMAIN-REGEX first-match-wins"
      pattern: "DOMAIN-REGEX.*bongacams\\.ru.*BG"
---

<objective>
Fix three known bugs in config.yaml: DST-PORT syntax error (DEDUP-08), OISD small duplicate (DEDUP-05), and bongacams.ru duplicate in Other category (DEDUP-09).

Purpose: Eliminate known parsing errors and dead code from the config. These are the lowest-risk fixes that do not change routing behavior -- only remove broken/redundant rules.
Output: Clean config.yaml passing mihomo syntax validation with no known bugs.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bugfixes/01-RESEARCH.md
@.planning/phases/01-bugfixes/01-CONTEXT.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DST-PORT syntax and remove bongacams.ru duplicate</name>
  <files>config.yaml</files>
  <action>
Two point-edits in config.yaml:

**Fix 1 -- DEDUP-08 (DST-PORT syntax):**
Find the rule `- DST-PORT,53,53` (around line 1603). Replace it with:
```yaml
  - AND,((DST-PORT,53)),53
```
This wraps the DST-PORT in an AND construct to eliminate the comma ambiguity between port separator and field separator. The AND pattern with DST-PORT is already used elsewhere in this config (line 1294). The proxy group '53' exists (line 452, defaults to DIRECT). Do NOT change position or the target group name.

Per user decision: group name '53' stays as-is, position stays at end of rules section.

**Fix 2 -- DEDUP-09 (bongacams.ru duplicate):**
Find and delete the line `- DOMAIN-SUFFIX,bongacams.ru,Other` (around line 1491). This is dead code: bongacams.ru is already caught by `DOMAIN-REGEX,^([\w\-\.]+\.)?bongacams\.ru$,BG` at line ~1309 (first-match-wins). Removing it does not change routing behavior.

Per user decision: only remove the duplicate from Other. Do NOT touch other adult-site entries in Other (onlyfans, pornhub etc.) -- those are Phase 3 scope.

Do NOT touch BG_in provider -- research confirmed bare DOMAIN-SUFFIX entries already cover .ru TLD.
  </action>
  <verify>
1. Search config.yaml for "DST-PORT,53,53" -- must return 0 results (old syntax gone)
2. Search config.yaml for "AND,((DST-PORT,53)),53" -- must return exactly 1 result
3. Search config.yaml for "bongacams.ru,Other" -- must return 0 results
4. Search config.yaml for "bongacams" -- must still return results in BG group rules (lines ~1297-1311) and BG_in provider (lines ~634-646)
  </verify>
  <done>DST-PORT rule uses AND-wrapper syntax. bongacams.ru removed from Other category but still routed through BG group.</done>
</task>

<task type="auto">
  <name>Task 2: Remove OISD small duplicate</name>
  <files>config.yaml</files>
  <action>
**Fix 3 -- DEDUP-05 (OISD small removal):**

Delete the oisd_small provider definition from rule-providers section (around lines 1221-1227):
```yaml
  oisd_small:
    type: http
    behavior: domain
    format: mrs
    url: https://github.com/legiz-ru/mihomo-rule-sets/raw/main/oisd/small.mrs
    path: ./oisd/small.mrs
    interval: 86400
```

Delete the oisd_small rule from rules section (around line 1607):
```yaml
  - RULE-SET,oisd_small,Ad-Filter
```

Both removals are required together -- removing only the provider without the rule reference (or vice versa) will cause mihomo to fail on startup.

LEAVE UNTOUCHED:
- `oisd_big` provider definition (lines ~1214-1220) -- this stays
- `RULE-SET,oisd_big,Ad-Filter` rule (line ~1608, will shift up after deletion) -- this stays
- `oisd_nsfw_big` if present -- not part of this fix

OISD big is a confirmed superset of OISD small (per OISD FAQ). No ad-blocking coverage is lost.
  </action>
  <verify>
1. Search config.yaml for "oisd_small" -- must return 0 results (provider and rule both gone)
2. Search config.yaml for "oisd_big" -- must return at least 2 results (provider definition + rule reference)
3. YAML syntax check: the file should be valid YAML (no indentation errors from deletion)
  </verify>
  <done>oisd_small completely removed from both rule-providers and rules sections. Only oisd_big remains for ad-blocking.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "DST-PORT,53,53" config.yaml` returns 0
2. `grep -c "AND,((DST-PORT,53)),53" config.yaml` returns 1
3. `grep -c "oisd_small" config.yaml` returns 0
4. `grep -c "oisd_big" config.yaml` returns >= 2
5. `grep -c "bongacams.ru,Other" config.yaml` returns 0
6. `grep -c "bongacams" config.yaml` returns > 0 (still present in BG rules)
7. Config is valid YAML (no parsing errors)

Note: `mihomo -t` validation cannot be run in this environment (mihomo runs on Keenetic router). The user should validate on the router after deploying.
</verification>

<success_criteria>
- All three bugs fixed with minimal, targeted edits
- No unintended changes to routing behavior
- Config remains valid YAML
- OISD big still provides ad-blocking coverage
- BG group still routes bongacams.ru traffic
- DST-PORT rule for port 53 uses unambiguous syntax
</success_criteria>

<output>
After completion, create `.planning/phases/01-bugfixes/01-01-SUMMARY.md`
</output>
