---
phase: 02-service-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.yaml]
autonomous: true
requirements: [DEDUP-01, DEDUP-04]

must_haves:
  truths:
    - "YouTube-правила не содержат inline DOMAIN-SUFFIX (кроме jnn-pa.googleapis.com), дубль RULE-SET удалён"
    - "Cloudflare-правила не содержат inline DOMAIN-SUFFIX, все 17 удалены"
    - "Осиротевшие провайдеры youtube-domains, youtube-ips, cloudflare_ips удалены из rule-providers"
    - "stable.dl2.discordapp.net удалён из YouTube-правил (ошибочная привязка)"
    - "mihomo config.yaml остаётся валидным YAML после всех изменений"
  artifacts:
    - path: "config.yaml"
      provides: "Дедуплицированные правила YouTube и Cloudflare"
      contains: "RULE-SET,youtube,YouTube"
  key_links:
    - from: "rules section"
      to: "rule-providers section"
      via: "RULE-SET references"
      pattern: "RULE-SET,(youtube|cloudflare)"
---

<objective>
Удаление дублирующихся правил YouTube (17 inline DOMAIN-SUFFIX + дубль RULE-SET + осиротевшие провайдеры) и Cloudflare (17 inline DOMAIN-SUFFIX + осиротевший провайдер). Оба сервиса имеют одинаковый паттерн: inline-правила полностью покрыты существующими GEOSITE/RULE-SET.

Purpose: Убрать ~35 мертвых inline-правил и 3 осиротевших провайдера, исправить ошибочную привязку Discord-домена к YouTube
Output: config.yaml с консолидированными YouTube (3-4 правила) и Cloudflare (2 правила) блоками
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-deduplication/02-RESEARCH.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: YouTube deduplication — удалить inline-дубли, дубль RULE-SET, осиротевшие провайдеры</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Удалить осиротевшие rule-provider (определены, но нет RULE-SET ссылки в rules):**
- Удалить провайдер `youtube-domains` (строки ~1070-1076, Anton111111 classical format) — нигде не используется
- Удалить провайдер `youtube-ips` (строки ~1077-1083, Anton111111 classical format) — нигде не используется

**Шаг 2: Удалить дубль RULE-SET в rules:**
- Удалить вторую строку `RULE-SET,youtube,YouTube` (строка ~1374) — дубль первой (строка ~1334)

**Шаг 3: Удалить 17 inline DOMAIN-SUFFIX для YouTube (строки ~1386-1402):**
- Удалить ВСЕ строки DOMAIN-SUFFIX,*,YouTube в этом блоке
- ВСЕ 17 доменов покрыты geosite youtube (179 доменов), кроме jnn-pa.googleapis.com
- ИСКЛЮЧЕНИЕ: `DOMAIN-SUFFIX,jnn-pa.googleapis.com,YouTube` — СОХРАНИТЬ как inline-дополнение (не покрыт geosite youtube напрямую, per user decision: лучше лишнее правило)
- УДАЛИТЬ `DOMAIN-SUFFIX,stable.dl2.discordapp.net,YouTube` (строка ~1391) — ЭТО ОШИБКА: Discord домен в YouTube группе. Домен покрыт Discord GEOSITE (discordapp.net есть в geosite discord.yaml)

**Шаг 4: Собрать YouTube правила в один блок рядом:**
Целевое состояние YouTube (вместо 21 правила → 4):
```yaml
  # --- YouTube: консолидировано из 21 правила ---
  - RULE-SET,youtube,YouTube                      # MetaCubeX geosite (179 доменов)
  - GEOSITE,youtube,YouTube                        # GEOSITE backup
  - PROCESS-NAME-REGEX,(?i).*youtube.*,YouTube     # Процессы
  # YouTube: inline-дополнение (не покрыт geosite youtube)
  - DOMAIN-SUFFIX,jnn-pa.googleapis.com,YouTube    # YouTube JNN push notification API
```

**Шаг 5: Удалить осиротевшие комментарии**, оставшиеся от удалённых inline-правил.

**Верификация после изменений:**
- grep по `youtube` в rules-секции: должно быть ровно 4 правила
- grep по `youtube-domains` и `youtube-ips` в rule-providers: должно быть 0 результатов
- grep по `stable.dl2.discordapp.net`: должно быть 0 результатов
  </action>
  <verify>
1. Grep `DOMAIN-SUFFIX.*YouTube` в rules — должен остаться только `jnn-pa.googleapis.com`
2. Grep `RULE-SET,youtube,YouTube` — ровно 1 вхождение (не 2)
3. Grep `youtube-domains` и `youtube-ips` в rule-providers — 0 результатов
4. Grep `stable.dl2.discordapp.net` — 0 результатов
5. YAML-парсинг config.yaml без ошибок (python -c "import yaml; yaml.safe_load(open('config.yaml'))")
  </verify>
  <done>YouTube определён 4 правилами (RULE-SET + GEOSITE + PROCESS-NAME + 1 inline-дополнение), без дублей и осиротевших провайдеров. Ошибочная привязка discordapp.net к YouTube исправлена.</done>
</task>

<task type="auto">
  <name>Task 2: Cloudflare deduplication — удалить 17 inline DOMAIN-SUFFIX и осиротевший провайдер</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Удалить осиротевший rule-provider:**
- Удалить провайдер `cloudflare_ips` (строка ~977-983, Anton111111) — определён, но RULE-SET,cloudflare_ips нигде не используется в rules

**Шаг 2: Удалить 17 inline DOMAIN-SUFFIX для Cloudflare (строки ~1404-1420):**
- Удалить ВСЕ 17 строк DOMAIN-SUFFIX,*,Cloudflare
- ВСЕ 17 доменов подтверждённо покрыты geosite cloudflare.yaml (74 домена)
- Список удаляемых: cloudflare-ech.com, encryptedsni.com, cloudflareaccess.com, cloudflareapps.com, cloudflarebolt.com, cloudflareclient.com, cloudflaredge.com, cloudflare-dns.com, cloudflaregallery.com, cloudflareinsights.com, cloudflareonelook.com, cloudflareresearch.com, cloudflarestatus.com, cloudflarestream.com, cloudflare.com, one.one.one.one, 1.1.1.1.one
- НЕ ТРОГАТЬ строку 1617 `DOMAIN,cloudflare-ech.com,🌍VPN` в ECH-секции — это Phase 4/v2 scope

**Шаг 3: Собрать Cloudflare правила в один блок:**
Целевое состояние Cloudflare (вместо 19 правил → 2):
```yaml
  # --- Cloudflare: консолидировано из 17 inline-правил ---
  - RULE-SET,cloudflare-ips,Cloudflare             # MetaCubeX geoip
  - RULE-SET,cloudflare-domains,Cloudflare         # MetaCubeX geosite (74 домена, покрывает все 17 inline)
```

**Шаг 4: Удалить осиротевшие комментарии** от удалённых inline-правил.

**Верификация:**
- grep `DOMAIN-SUFFIX.*Cloudflare` в rules: 0 результатов
- grep `cloudflare_ips` в rule-providers: 0 результатов
- RULE-SET,cloudflare-domains и RULE-SET,cloudflare-ips в rules: по 1 вхождению
  </action>
  <verify>
1. Grep `DOMAIN-SUFFIX.*Cloudflare` в rules — 0 результатов
2. Grep `cloudflare_ips` в rule-providers — 0 результатов (осиротевший удалён)
3. Grep `RULE-SET,cloudflare` в rules — ровно 2 (cloudflare-ips + cloudflare-domains)
4. Строка `DOMAIN,cloudflare-ech.com,🌍VPN` в ECH-секции НЕ тронута
5. YAML-парсинг config.yaml без ошибок
  </verify>
  <done>Cloudflare определён 2 правилами (RULE-SET для IP + RULE-SET для доменов), все 17 inline-дублей и осиротевший провайдер удалены. ECH-секция не затронута.</done>
</task>

</tasks>

<verification>
1. YouTube: ровно 4 правила в rules (RULE-SET + GEOSITE + PROCESS-NAME + jnn-pa inline)
2. Cloudflare: ровно 2 правила в rules (cloudflare-ips + cloudflare-domains)
3. Удалены ~35 inline-строк (17 YouTube + 17 Cloudflare + 1 дубль RULE-SET)
4. Удалены 3 осиротевших провайдера (youtube-domains, youtube-ips, cloudflare_ips)
5. stable.dl2.discordapp.net удалён из YouTube
6. YAML валиден, все RULE-SET ссылки имеют соответствующие провайдеры
</verification>

<success_criteria>
- config.yaml парсится как валидный YAML
- YouTube-правил ровно 4 (было 21)
- Cloudflare-правил ровно 2 (было 19)
- Ноль осиротевших провайдеров для YouTube и Cloudflare
- Ноль inline DOMAIN-SUFFIX для YouTube (кроме jnn-pa) и Cloudflare
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-deduplication/02-01-SUMMARY.md`
</output>
