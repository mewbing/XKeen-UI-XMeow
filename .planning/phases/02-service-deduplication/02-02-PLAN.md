---
phase: 02-service-deduplication
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [config.yaml]
autonomous: true
requirements: [DEDUP-02, DEDUP-03]

must_haves:
  truths:
    - "Discord определён через консолидированный набор правил без дублирующих inline DOMAIN-SUFFIX (кроме 3 непокрытых geosite)"
    - "Telegram определён через консолидированный набор правил без дублирующего провайдера telegram-domains"
    - "Дублирующий rule-provider telegram-domains (дефис) удалён, все ссылки используют telegram_domains (подчёркивание)"
    - "Осиротевший провайдер discord_ips удалён"
    - "Inline провайдер discord удалён (покрыт discord_vc + discord_domains + OR-блок)"
  artifacts:
    - path: "config.yaml"
      provides: "Дедуплицированные правила Discord и Telegram"
      contains: "RULE-SET,discord_domains,Discord"
  key_links:
    - from: "rules section"
      to: "rule-providers section"
      via: "RULE-SET references"
      pattern: "RULE-SET,(discord|telegram)"
---

<objective>
Удаление дублирующихся правил Discord (30+ правил → 7) и Telegram (10 правил → 7). Discord сложнее: включает voice IP дубли, inline провайдер, 24 inline DOMAIN-SUFFIX. Telegram: дублирующиеся провайдеры с одинаковым URL, лишний DOMAIN-REGEX.

Purpose: Консолидировать два самых сложных сервиса, убрать ~35 мертвых правил, 3 лишних провайдера
Output: config.yaml с чистыми блоками Discord (7 правил) и Telegram (7 правил)
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-deduplication/02-RESEARCH.md
@.planning/phases/02-service-deduplication/02-01-SUMMARY.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discord deduplication — удалить inline-дубли, консолидировать voice IP, удалить осиротевшие провайдеры</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Удалить осиротевшие/лишние rule-provider:**
- Удалить провайдер `discord_ips` (строки ~1100-1106, Anton111111) — определён, но RULE-SET,discord_ips нигде не используется
- Удалить inline провайдер `discord` (строки ~1127-1135) — содержимое покрыто discord_vc + discord_domains + DOMAIN-KEYWORD в OR-блоке. Если в rules есть ссылка `RULE-SET,discord,Discord`, удалить и её (строка ~1350)

**Шаг 2: Удалить дубль RULE-SET,discord_domains:**
- Строка ~1383: `RULE-SET,discord_domains,Discord` — ДУБЛЬ, уже есть в OR-блоке строки ~1336
- Удалить строку 1383

**Шаг 3: Удалить 24 inline DOMAIN-SUFFIX для Discord (строки ~1421-1444):**
- Удалить ВСЕ строки DOMAIN-SUFFIX,*,Discord в этом блоке
- 21 из 24 доменов покрыты geosite discord.yaml (28 доменов)
- СОХРАНИТЬ 3 домена НЕ покрытых geosite как inline-дополнения:
  - `DOMAIN-SUFFIX,discord.app,Discord`
  - `DOMAIN-SUFFIX,discord.status,Discord`
  - `DOMAIN-SUFFIX,discordsez.com,Discord`

**Шаг 4: Собрать Discord правила в один блок:**
Целевое состояние Discord (вместо 30+ правил → 7):
```yaml
  # --- Discord: консолидировано из 30+ правил ---
  - OR,((RULE-SET,discord_domains),(RULE-SET,discord_voiceips),(PROCESS-NAME,Discord.exe)),Discord
  - RULE-SET,discord_vc,Discord                    # Voice IP ranges (inline AND-конструкции, консервативно сохранён)
  - AND,((RULE-SET,cloudflare-ips),(NETWORK,udp),(DST-PORT,19200-19500)),Discord  # CF media ports
  - PROCESS-NAME-REGEX,(?i).*discord.*,Discord     # Процессы
  # Discord: inline-дополнения (не покрыты geosite discord):
  - DOMAIN-SUFFIX,discord.app,Discord
  - DOMAIN-SUFFIX,discord.status,Discord
  - DOMAIN-SUFFIX,discordsez.com,Discord
```

Примечание: Строка с AND,discord_voiceips,UDP,50000-50100 (строка ~1381) УДАЛЯЕТСЯ — трафик уже покрыт OR-блоком (discord_voiceips в OR) и discord_vc (inline AND). Тройное покрытие избыточно.

**Шаг 5: Удалить осиротевшие комментарии** от удалённых inline-правил.
  </action>
  <verify>
1. Grep `DOMAIN-SUFFIX.*Discord` в rules — ровно 3 (discord.app, discord.status, discordsez.com)
2. Grep `discord_ips` в rule-providers — 0 результатов (осиротевший удалён)
3. Grep `discord` inline-провайдер в rule-providers — 0 результатов (inline удалён)
4. Grep `RULE-SET,discord_domains,Discord` — ровно 0 отдельных (только внутри OR-блока)
5. OR-блок Discord цел и содержит discord_domains + discord_voiceips + PROCESS-NAME
6. YAML-парсинг config.yaml без ошибок
  </verify>
  <done>Discord определён 7 правилами (OR-блок + discord_vc + AND/CF-media + PROCESS-NAME-REGEX + 3 inline-дополнения). Удалены: 24 inline DOMAIN-SUFFIX, дубль discord_domains, RULE-SET,discord inline провайдер, осиротевший discord_ips, лишний AND с discord_voiceips.</done>
</task>

<task type="auto">
  <name>Task 2: Telegram deduplication — удалить дублирующийся провайдер, лишний DOMAIN-REGEX, дубль RULE-SET</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Удалить дублирующийся rule-provider:**
- Удалить провайдер `telegram-domains` (строки ~856-862, MetaCubeX geosite/telegram.mrs) — ДУБЛЬ провайдера `telegram_domains` (строка ~1264, ОДИНАКОВЫЙ URL)
- Предпочтение: оставить `telegram_domains` (подчёркивание), т.к. используется в OR-блоке (строка ~1335)
- Если есть ссылки `RULE-SET,telegram-domains,Telegram` в rules (строка ~1377), удалить их — трафик покрыт telegram_domains в OR-блоке

**Шаг 2: Удалить лишние правила в rules:**
- Удалить `DOMAIN-REGEX,^([\w\-\.]+\.)?antarcticwallet\.com$,Telegram` (строка ~1309) — покрыт `DOMAIN-SUFFIX,antarcticwallet,Telegram` (строка ~1296), DOMAIN-SUFFIX матчит все поддомены
- Удалить `RULE-SET,telegram-domains,Telegram` (строка ~1377) — дубль telegram_domains в OR-блоке

**Шаг 3: Собрать Telegram правила в один блок:**
Целевое состояние Telegram (вместо 10 правил → 7):
```yaml
  # --- Telegram: консолидировано из 10 правил ---
  - DOMAIN-SUFFIX,antarcticwallet,Telegram         # TON Wallet (не покрыт geosite telegram)
  - OR,((RULE-SET,telegram_ips),(RULE-SET,telegram_domains)),Telegram  # IP + домены MetaCubeX
  - RULE-SET,telegram-ips,Telegram                 # Anton111111 IP list (дополнительное покрытие)
  - GEOSITE,telegram,Telegram                      # GEOSITE backup
  - GEOIP,telegram,Telegram                        # GeoIP backup
  - PROCESS-NAME-REGEX,(?i).*ayugram.*,Telegram    # Ayugram клиент
  - PROCESS-NAME-REGEX,(?i).*telegram.*,Telegram   # Telegram клиент
```

**Шаг 4: Удалить осиротевшие комментарии.**
  </action>
  <verify>
1. Grep `telegram-domains` в rule-providers — 0 результатов (дубль удалён)
2. Grep `telegram_domains` в rule-providers — ровно 1 (оставлен)
3. Grep `DOMAIN-REGEX.*antarcticwallet` — 0 результатов
4. Grep `RULE-SET,telegram-domains` в rules — 0 результатов
5. Grep `DOMAIN-SUFFIX,antarcticwallet` — ровно 1 (сохранён)
6. OR-блок Telegram цел: OR,((RULE-SET,telegram_ips),(RULE-SET,telegram_domains)),Telegram
7. YAML-парсинг config.yaml без ошибок
  </verify>
  <done>Telegram определён 7 правилами (DOMAIN-SUFFIX antarcticwallet + OR-блок + telegram-ips + GEOSITE + GEOIP + 2 PROCESS-NAME). Удалены: дубль провайдера telegram-domains, лишний DOMAIN-REGEX, дубль RULE-SET в rules.</done>
</task>

</tasks>

<verification>
1. Discord: 7 правил в rules (OR-блок + discord_vc + AND/CF + PROCESS-NAME-REGEX + 3 inline)
2. Telegram: 7 правил в rules (antarcticwallet + OR + telegram-ips + GEOSITE + GEOIP + 2 PROCESS-NAME)
3. Удалены ~30 строк правил (24 Discord inline + дубли + 3 Telegram дубля)
4. Удалены 3 провайдера (discord_ips, discord inline, telegram-domains)
5. YAML валиден, все RULE-SET ссылки имеют соответствующие провайдеры
</verification>

<success_criteria>
- config.yaml парсится как валидный YAML
- Discord-правил 7 (было 30+)
- Telegram-правил 7 (было 10)
- Ноль дублирующихся провайдеров для Telegram
- Ноль осиротевших провайдеров для Discord
- OR-блоки обоих сервисов сохранены без изменений
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-deduplication/02-02-SUMMARY.md`
</output>
