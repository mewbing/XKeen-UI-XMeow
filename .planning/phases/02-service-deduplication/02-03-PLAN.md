---
phase: 02-service-deduplication
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [config.yaml]
autonomous: true
requirements: [DEDUP-06, DEDUP-07]

must_haves:
  truths:
    - "Торрент-правило OR-блок присутствует ровно 1 раз (дубль удалён)"
    - "refilter_ipsum привязан к ECH-Refilter (не к Комьюнити), ровно 1 вхождение"
    - "Все правила каждого сервиса сгруппированы рядом в rules-секции"
    - "mihomo config.yaml остаётся валидным YAML"
  artifacts:
    - path: "config.yaml"
      provides: "Финальный дедуплицированный конфиг с правилами, сгруппированными по сервисам"
      contains: "RULE-SET,refilter_ipsum,ECH-Refilter"
  key_links:
    - from: "rules section torrent block"
      to: "rule-providers torrent-clients, torrent-trackers"
      via: "RULE-SET references in OR block"
      pattern: "OR.*torrent-clients.*torrent-trackers.*DIRECT"
    - from: "rules section refilter block"
      to: "rule-providers refilter_ipsum"
      via: "RULE-SET reference"
      pattern: "RULE-SET,refilter_ipsum,ECH-Refilter"
---

<objective>
Удаление дублей торрент-правил (дословный дубль OR-блока) и консолидация refilter_ipsum (два вхождения в разные группы → одно в ECH-Refilter). Финальная перегруппировка всех rules по сервисам для читаемости.

Purpose: Завершить дедупликацию Phase 2, обеспечить чистую группировку правил по сервисам
Output: config.yaml — полностью дедуплицированный, правила сгруппированы по сервисам
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-service-deduplication/02-RESEARCH.md
@.planning/phases/02-service-deduplication/02-01-SUMMARY.md
@.planning/phases/02-service-deduplication/02-02-SUMMARY.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Торренты + refilter_ipsum — удалить дубли</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Удалить дубль торрент OR-блока:**
- Строка ~1603 содержит дословный дубль строки ~1331: `OR,((RULE-SET,torrent-clients),(RULE-SET,torrent-trackers)),DIRECT`
- Удалить строку ~1603 (второе вхождение — мертвый код из-за first-match-wins)
- Первое вхождение (~1331) остаётся

Целевое состояние торрентов:
```yaml
  # --- Торренты ---
  - OR,((RULE-SET,torrent-clients),(RULE-SET,torrent-trackers)),DIRECT
  - PROCESS-NAME-REGEX,(?i).*torrent.*,DIRECT
  - RULE-SET,torrent-websites,🌍VPN
```

**Шаг 2: Консолидация refilter_ipsum:**
- Строка ~1589: `RULE-SET,refilter_ipsum,Комьюнити` — УДАЛИТЬ (первое вхождение, перехватывает трафик)
- Строка ~1621: `RULE-SET,refilter_ipsum,ECH-Refilter` — ОСТАВИТЬ
- Обоснование (Claude's Discretion): refilter_ipsum — IP-правила заблокированных ресурсов, связанные с ECH-логикой. ECH-Refilter — переключаемая группа (VPN/DIRECT), даёт пользователю контроль. Контекст конфига: refilter_ipsum расположен рядом с refilter_ech и refilter_noech — логическая группировка ECH-фильтрации.
- ВАЖНО: Удаление строки 1589 ИЗМЕНИТ поведение маршрутизации! Ранее трафик шёл в Комьюнити, теперь пойдёт в ECH-Refilter. Это осознанное решение, задокументированное в CONTEXT.md (Claude's Discretion) и подтверждённое в RESEARCH.md.

Целевое состояние refilter ECH-блока:
```yaml
  # --- Refilter ECH-логика ---
  - RULE-SET,refilter_ech,DIRECT
  - RULE-SET,refilter_noech,ECH-Refilter
  - RULE-SET,refilter_ipsum,ECH-Refilter   # консолидировано: был дубль в Комьюнити
```
  </action>
  <verify>
1. Grep `OR.*torrent-clients.*torrent-trackers.*DIRECT` — ровно 1 вхождение
2. Grep `RULE-SET,refilter_ipsum` — ровно 1 вхождение (ECH-Refilter)
3. Grep `refilter_ipsum.*Комьюнити` — 0 результатов
4. YAML-парсинг config.yaml без ошибок
  </verify>
  <done>Торрент OR-блок без дубля (1 вхождение). refilter_ipsum привязан к ECH-Refilter (1 вхождение). Поведение маршрутизации refilter_ipsum осознанно изменено с Комьюнити на ECH-Refilter.</done>
</task>

<task type="auto">
  <name>Task 2: Финальная верификация и перегруппировка правил по сервисам</name>
  <files>config.yaml</files>
  <action>
**Шаг 1: Проверить, что все сервисные правила сгруппированы.**

После Plans 01 и 02 каждый сервис уже должен быть собран в блок. Проверить, что нет разбросанных правил одного сервиса по разным местам rules-секции. Если есть — переместить к основному блоку сервиса.

Сервисы для проверки: YouTube, Discord, Telegram, Cloudflare, Торренты, refilter.

**Шаг 2: Добавить комментарии-разделители между сервисными блоками** (per user decision: краткие комментарии на русском):
```yaml
  # --- YouTube: консолидировано из 21 правила ---
  # --- Discord: консолидировано из 30+ правил ---
  # --- Telegram: консолидировано из 10 правил ---
  # --- Cloudflare: консолидировано из 17 inline-правил ---
  # --- Торренты ---
  # --- Refilter ECH-логика ---
```

Если комментарии уже добавлены в Plans 01 и 02, не дублировать.

**Шаг 3: Порядок сервисов внутри rules-секции (Claude's Discretion):**
Сохранить текущий относительный порядок первых вхождений сервисов (per RESEARCH.md Open Question 3): QUIC/SAFE → Adult → Torrent → YouTube → Telegram → Discord → ... → Cloudflare → RU трафик → Refilter → OISD → CDN IPs. НЕ менять порядок между сервисами — только группировать правила ВНУТРИ каждого сервиса.

**Шаг 4: Финальная санитарная проверка всего rules и rule-providers:**
- Для каждого RULE-SET,X в rules — проверить что X определён в rule-providers
- Для каждого провайдера в rule-providers — проверить что хотя бы один RULE-SET,X ссылается на него в rules (иначе осиротевший)
- Если найдены расхождения — исправить (удалить осиротевших, добавить недостающих провайдеров)

**Шаг 5: Подсчёт результатов для SUMMARY:**
- Сколько правил было до Phase 2 (в rules-секции)
- Сколько правил стало после Phase 2
- Сколько провайдеров удалено
- Сколько строк удалено из конфига
  </action>
  <verify>
1. Каждый из 6 сервисов (YouTube, Discord, Telegram, Cloudflare, Торренты, Refilter) — все правила сгруппированы в одном месте
2. Каждый RULE-SET,X в rules имеет определение X в rule-providers
3. Каждый провайдер в rule-providers используется хотя бы в одном RULE-SET в rules
4. YAML-парсинг config.yaml без ошибок
5. python -c "import yaml; yaml.safe_load(open('config.yaml'))" — exit code 0
  </verify>
  <done>Все правила Phase 2 сгруппированы по сервисам, комментарии-разделители добавлены, нет осиротевших провайдеров, нет висящих RULE-SET ссылок. Конфиг полностью дедуплицирован.</done>
</task>

</tasks>

<verification>
1. Торрент OR-блок: 1 вхождение (дубль удалён)
2. refilter_ipsum: 1 вхождение в ECH-Refilter
3. Все 6 сервисов сгруппированы по блокам в rules
4. 0 осиротевших провайдеров в rule-providers
5. 0 висящих RULE-SET ссылок без провайдера
6. YAML валиден
7. Общий итог Phase 2: ~70 строк правил удалено, 6 провайдеров удалено
</verification>

<success_criteria>
- config.yaml парсится как валидный YAML
- Торрент OR-блок ровно 1 (было 2)
- refilter_ipsum ровно 1 в ECH-Refilter (было 2 в разных группах)
- Правила каждого сервиса — в одном месте (не разбросаны)
- Итоговый подсчёт: Phase 2 сократила ~90 правил до ~30
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-deduplication/02-03-SUMMARY.md`
</output>
