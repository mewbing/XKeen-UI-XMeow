---
phase: 04-url-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.yaml]
autonomous: false
requirements: [URL-01, URL-02, URL-03, URL-04]

must_haves:
  truths:
    - "All 16 Anton111111 URLs point to latest release tag (lists-20260222-003136-835e3fe)"
    - "All HTTP rule-provider URLs return HTTP 200 with non-empty valid content"
    - "Every rule-provider defined in rule-providers is referenced in rules (0 orphans)"
    - "Every RULE-SET reference in rules has a corresponding rule-provider definition (0 missing)"
    - "Dashboard password line has a TODO comment for user to change"
  artifacts:
    - path: "config.yaml"
      provides: "Updated config with fresh Anton111111 URLs and password TODO"
      contains: "lists-20260222-003136-835e3fe"
  key_links:
    - from: "rule-providers (Anton111111 URLs)"
      to: "GitHub releases"
      via: "HTTP download URL with release tag"
      pattern: "lists-20260222-003136-835e3fe"
---

<objective>
Update all external URLs in config.yaml to their latest versions, verify every rule-provider URL is accessible (HTTP 200), confirm zero orphan providers, and mark dashboard password for user change.

Purpose: Ensure all external data sources are current and accessible, eliminating stale/broken links.
Output: Updated config.yaml with fresh URLs and all providers verified.
</objective>

<execution_context>
@C:/Users/ART-PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ART-PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-url-audit-cleanup/04-RESEARCH.md
@.planning/phases/04-url-audit-cleanup/04-CONTEXT.md
@config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Anton111111 URLs and add password TODO</name>
  <files>config.yaml</files>
  <action>
    1. **Update Anton111111 release tag** (16 URLs):
       Replace all occurrences of `lists-20251102-014123-835e3fe` with `lists-20260222-003136-835e3fe` in config.yaml.
       This is a simple string find-and-replace. All 16 file names (akamai_ips.list, amazon_ips.list, cdn77_ips.list, digitalocean_ips.list, fastly_ips.list, google_ips.list, hetzner_ips.list, mega_ips.list, meta_ips.list, oracle_ips.list, ovh_ips.list, vultr_ips.list, other.list, politic.list, telegram_ips.list, ru_ips.list) are confirmed identical in both old and new releases.

    2. **Add TODO comment for dashboard password** on line 25:
       Change:
       ```yaml
       secret: 'admin' # Пароль для доступа к Web-интерфейсу. Рекомендуется использовать сложный пароль.
       ```
       To:
       ```yaml
       secret: 'admin' # TODO: Сменить пароль! Задайте сложный пароль вручную (не 'admin').
       ```
       Per CONTEXT.md: password is set by user manually, not auto-generated. Plaintext storage is OK for local router.

    3. **Verify no other GitHub URLs use pinned tags/commits** (non-`main`/`meta`/`latest`):
       - MetaCubeX URLs: all on `meta` branch (auto-updating) -- OK
       - legiz-ru URLs: all on `main` branch (auto-updating) -- OK
       - itdoginfo URLs: all on `main` branch -- OK
       - geox-url: uses `releases/latest/download` -- OK
       - external-ui-url: uses `releases/latest/download` -- OK
       If any additional pinned-tag URLs are found, report them but do NOT change (out of scope).
  </action>
  <verify>
    - grep config.yaml for old tag: `grep "lists-20251102" config.yaml` should return 0 results
    - grep config.yaml for new tag: `grep "lists-20260222" config.yaml` should return exactly 16 results
    - grep config.yaml for TODO password: `grep "TODO.*Сменить" config.yaml` should return 1 result
  </verify>
  <done>
    All 16 Anton111111 URLs updated to latest release. Password line has TODO comment. No other pinned URLs found.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all HTTP rule-provider URLs and orphan check</name>
  <files>config.yaml</files>
  <action>
    1. **Extract all HTTP URLs from rule-providers section** of config.yaml (skip inline providers, skip proxy-provider subscription URL, skip health-check URLs).

    2. **Test each URL for HTTP 200** using curl/fetch. For each URL:
       - Send HTTP GET request
       - Verify response status is 200
       - Verify response body is non-empty (at least 10 bytes)
       - Record result (OK / FAIL / EMPTY)

       Expected URL groups to check:
       - 16x Anton111111/rule-lists (newly updated URLs)
       - 18x MetaCubeX/meta-rules-dat
       - 13x legiz-ru/mihomo-rule-sets
       - 2x itdoginfo/allow-domains
       Total: ~49 HTTP URLs

    3. **If any URL returns non-200 or empty content:**
       Per CONTEXT.md: look for replacement URL (same ruleset from alternative source). If no replacement found, add `# BROKEN: HTTP {status}` comment before the URL line. Leave for manual review.

    4. **Orphan provider check:**
       Parse all provider names from rule-providers section, parse all RULE-SET references from rules section. Confirm:
       - 0 providers defined but not used (orphans)
       - 0 RULE-SET references without matching provider (missing)
       Phase 2 already cleaned this up (62 providers = 62 references). This step confirms no regression.

    5. **Report results** as a summary table.
  </action>
  <verify>
    - All HTTP URLs return 200 (or are annotated as BROKEN)
    - Orphan check: 0 orphans, 0 missing
    - Summary table logged
  </verify>
  <done>
    Every rule-provider URL verified accessible. Zero orphan providers confirmed. Any broken URLs annotated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies URL audit results</name>
  <files>config.yaml</files>
  <action>
    Present the URL verification summary to the user. Show:
    - Updated 16 Anton111111 URLs from tag `lists-20251102-014123-835e3fe` to `lists-20260222-003136-835e3fe`
    - Verified all ~49 HTTP rule-provider URLs return HTTP 200
    - Confirmed 0 orphan providers, 0 missing providers
    - Added TODO comment for dashboard password change
    User reviews results and decides to approve or request changes.
    **Separately:** User changes dashboard password in config.yaml (line `secret: 'admin'`) to a strong password of their choice.
  </action>
  <verify>
    User types "approved" or describes issues to address.
  </verify>
  <done>
    User has reviewed and approved all URL changes and verification results.
  </done>
</task>

</tasks>

<verification>
- [ ] `grep "lists-20251102" config.yaml` returns 0 matches (old tag gone)
- [ ] `grep "lists-20260222" config.yaml` returns 16 matches (new tag in place)
- [ ] All HTTP rule-provider URLs return HTTP 200 with non-empty content
- [ ] 62 providers defined, 62 RULE-SET references, 0 orphans, 0 missing
- [ ] `secret: 'admin'` line has TODO comment for password change
</verification>

<success_criteria>
URL-01: Anton111111 URLs updated to latest release -- DONE
URL-02: Orphan check confirms 0 unused providers -- DONE
URL-03: All URLs verified HTTP 200 -- DONE
URL-04: Password TODO added, user reminded to change manually -- DONE
</success_criteria>

<output>
After completion, create `.planning/phases/04-url-audit-cleanup/04-01-SUMMARY.md`
</output>
